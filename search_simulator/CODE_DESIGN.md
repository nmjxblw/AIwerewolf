# Search Simulator 代码设计与变更记录

## 文档职责

本文保留历史工程映射和上下文：文件、数据结构、接口、依赖、迁移、兼容性与验证结果。

当前算法假设、数学公式、状态语义和设计不变量只写入 `ALGORITHM_DESIGN_V2.md`；V1 只作历史归档。战术落地难点补充在 `STRATEGY_IMPLEMENTATION.md`。当前工程改动、当前依赖和最新验证以 `CODE_CHANGES.md` 为准；本文件的旧日期、测试数量和历史验证记录不得覆盖增量文档中的新事实。

## 2026-08-23：分层持久化与已有解优先加载

- SQLite 增加 `lru`、`memory`、`solution` 三张职责表；旧 `state_signatures` 与 `simulation_runs` 通过 Core 批量迁移到新索引层，既有图表不复制、不删除；
- `lru` 负责状态签名热缓存，`memory` 负责运行/断点摘要，`solution` 只登记完整性校验通过的站位结果索引；搜索层通过持久化 API 查询，不接触 cursor 或明文 SQL；
- 每次运行先查询规范 solution 配置。命中时返回 `loaded_solution` 摘要，不启动 worker；GUI 将下一次点击标记为显式 `force_recompute`，CLI 使用同名开关重新创建 run；
- solution 配置键排除只影响 interval 观测的 `lambda_risk`，并向 GUI 传递来源 lambda 以决定是否需要动态回传；
- 新增 `list_memory_runs`、`list_solution_runs` 查询 API，保留旧图按站位分页读取；
- 最新验证：模块测试 37 项通过，持久化/入口/GUI/i18n/树搜索 ruff 检查通过，compileall 通过；4 人 1 狼 DFS CLI 验证首次计算、第二次 `SOLUTION_LOADED` 和显式 `--force_recompute` 重算，Pygame 无显示启动通过。

## 2026-08-23：Pygame 与智能零和博弈重构

### 状态

站位串行检查点、自动恢复、内存保护、显式参数传递、终态日志和 GUI 弹窗已经完成，并通过模块、CLI 与 Pygame 启动回归。

### 2026-08-23：单站位串行检查点与内存保护

- 全站位协调器只保留一个隔离计算 worker 和一个当前站位 Future；上一个站位的节点、边、摘要全部写入 SQLite 并提交运行检查点后，才创建下一个站位任务；
- `parallel_workers` 作为旧 CLI/配置兼容字段保留，但站位层实际并发固定为 1，GUI 不再把该字段解释为可并行站位数；
- 运行配置使用稳定 JSON 规范化后查找最近的 `running` / `interrupted` 批次；恢复时通过 SQLAlchemy Core API 读取站位摘要，并以持久化节点数、边数分别等于摘要计数作为完整性条件；
- 每个完整站位结束后更新运行摘要检查点。启动恢复时跳过完整站位；残留的半写入站位由同一持久化 API 清理并重新计算；
- 纯标准库内存守卫定期读取系统总物理内存与可用物理内存。低于固定比例与固定容量两者中的较高安全线时抛出可恢复中断，父进程停止派发、关闭结果写入链路并写入 `interrupted` 状态；
- `KeyboardInterrupt` 与内存守卫中断采用同一可恢复收尾路径；普通计算/持久化缺陷仍记录为 `failed`，避免把真实错误静默伪装为可恢复内存中断；
- 自动恢复不保存未完成站位的 frontier。站位内部中断时丢弃不完整节点/边，仅重算该站位；这是为了保证最终 DAG、路径回传和 interval 完整性。
- 本轮新增的内存守卫、配置匹配、完整性查询、检查点提交和串行调度函数均使用中文 docstring；关键事务顺序与恢复判定补充中文行内注释，并将该要求固化到模块 `AGENTS.md`。
- 跨进程 worker 构造不再把配置映射整体隐式展开；角色配置、搜索模式、战术、lambda、内存保留线和队列句柄均由稳定具名字段逐项读取并显式传入模拟器。参数语义与返回值使用中文文档注释说明。
- 实际验证：26 项模块测试通过；5 人无神职全部站位 DFS/BFS 都得到 95 个状态、600 条好人路径和 3600 条狼人路径；CLI 人工提高内存安全线后以 0 个站位进入 `interrupted`，降低安全线重启时复用同一 `run_id` 并完成站位；请求 4 worker 的串行 DFS 连续运行 10 次均退出 0，总耗时约 50.35 秒；Pygame 五帧无显示启动退出 0。
- 默认七人五战术完整迭代在本轮优化前曾同时展开 4 个站位，四个 worker 合计私有内存约 92 GB，且首个站位尚未完成。该旧运行已按用户要求终止，数据库和日志保留；优化完成后未擅自重新启动默认完整迭代。

### 2026-08-23：运行终态日志与 GUI 弹窗

- 根 logger 新增 UTF-8 文件 handler，默认写入模块运行日志；控制台格式继续保留。日志路径支持环境变量覆盖，并在启动时输出；
- 批处理协调器对 `complete`、内存保护 `interrupted`、用户 `KeyboardInterrupt`、普通异常和进程池崩溃分别输出 INFO、WARNING、ERROR/CRITICAL 日志。所有终态日志统一包含 `run_id`、检查点数量、总站位和下一站位；
- 启动入口固定创建模块根目录 `crash_log/`，使用一个带微秒的本地运行时间戳生成本次 crash 文件路径并写入环境，文件名不包含 PID；计算 worker 继承同一路径后安装 faulthandler，使 Windows access violation 即使无法抛出 Python 异常，也能写入本次会话专属日志。日志正文和 faulthandler header 保留实际 PID/线程信息，父进程收到损坏进程池异常后再写一条带恢复信息的 CRITICAL 运行日志；
- Pygame 使用模态消息窗口显示完成、中断、失败三种终态。异常从后台线程传回时携带格式化 traceback 和日志路径，主界面状态与弹窗文案保持一致；
- 针对附件中 GUI/SQLAlchemy 所在进程被 access violation 直接终止的情况，启动时扫描 `crash_log/` 中最近的非空历史文件；若其尚未写入已通知标记，Pygame 初始化后补发“上次运行原生崩溃”弹窗。该机制不声称能在已经死亡的进程内绘制 UI；
- 新增测试覆盖时间戳 crash 路径、历史原生崩溃一次性提示、互斥终态摘要、异常 traceback、worker 进程池崩溃分类和三类 GUI 弹窗内容；
- 实际验证：31 项模块测试通过；CLI 先触发内存保护 `interrupted`、再使用同一 `run_id` 恢复为 `complete`，运行日志分别包含两种明确终态且中断摘要不再使用“树搜索完成”；每次启动生成一个不含 PID 的时间戳 crash 文件，正文和运行日志保留父/worker PID；强制 Python 启动异常退出码为 1，runtime 日志包含 CRITICAL 与 traceback，crash 日志包含异常和 PID；三类弹窗文案测试通过，Pygame 完成弹窗五帧无显示 smoke 退出 0。

### 2026-08-23：捕获异常落盘与状态签名稳定性修复

- 附件中的 `AttributeError: 'dict_items' object has no attribute 'is_alive'` 是父进程从 worker 接收的可捕获 Python 异常，因此原实现只写 runtime logger，未触发 worker 的 `sys.excepthook`，本次时间戳 crash 文件保持为零字节；修复后，任何 `failed` 收尾都会通过统一 API 把 PID、线程、`run_id`、检查点、下一站位和完整异常链追加到同一个 crash 文件，GUI 对搜索外的已捕获异常也使用同一兜底 API，并用异常标记避免重复写入。
- 对默认状态签名热路径进行独立复现后，CPython 3.12.10 在递归闭包、`nonlocal` 双 64 位混合函数连续调用约十万次时可出现 `int` 变为 `cell`、`Player` 变为 `dict_items` 等对象错位，随后以 Windows `0xC0000005` 退出；因此该现象不是合法 GameState 写入了错误玩家类型。
- 状态签名改为显式栈驱动的非递归规范编码，再由标准 128 位摘要生成十六进制签名；参与签名的 GameState 字段集合不变，不引入 reward、interval、父节点或 UI 字段。新增百万次确定性签名压力测试，以及全部转移玩家类型契约回归。
- 验证结果：状态签名独立进程连续计算 1,000,000 次退出 0；默认五类战术的七人首夜与首个白天状态全部转移通过 Player 契约检查；34 项模块测试通过；CLI 使用五人一狼、预言家、女巫、默认智能投票与五类战术完成单站位 DFS，得到 14,037 个状态、25,429 条边并以 `status=complete` 退出。模拟 `BrokenProcessPool` 时，同一时间戳 crash 文件包含 PID、`run_id`、恢复位置和完整异常文本。

### 2026-08-23：interval 后处理可视化

- 单站位搜索在 frontier 清空后继续发布路径计数、节点 interval 逆拓扑回传和边 interval 同步三类阶段事件；事件携带当前站位、阶段、已处理量、总量和运行时，首尾强制发送，中间按 0.5 秒节流；
- GUI 在状态栏和站位行显示后处理阶段及处理计数，不把后处理比例混同于全站位搜索进度。暂停事件在分块边界继续生效；
- 用户拖动 λ 后，选中 DAG 的动态 interval 重算移出 Pygame 绘制调用栈，后台任务通过 UI 事件报告进度；连续拖动只保留最新 λ 请求，避免并发修改同一图；
- 暂不引入 NumPy。当前节点是字典记录、邻接关系为变长边索引，NumPy 化需要额外复制节点端点和邻接数组，增加百万级 DAG 的峰值内存；现有算法已是单遍常数额外空间，先以进度可观测性和分块响应解决“卡死”观感。
- 验证结果：37 项模块测试通过，覆盖路径阶段、邻接准备、节点 interval、边 interval、动态 λ 后台任务和 GUI 状态行；Pygame dummy 驱动连续渲染 3 帧并显示“节点区间回传 30/45”；四人单站位 DFS CLI 以 10 个状态、12 条边正常完成。pytest、CLI、ruff 和字节码验证产生的临时目录、数据库、结果文件及缓存均在各次验证后删除。

### 2026-08-23：Windows 区间回传稳定性与 Hover 文本修正

- 节点 Hover 从格式化 JSON 改为中文语义分区，完整覆盖节点观测、对局进度、公开/私有信息、投票/战术、站位标识和每名玩家的角色、生存与全部技能字段；
- DAG interval 回传改为显式累加器与顺序循环，搜索完成路径不再叠加子节点 ID 和四元边界的生成器帧；
- 搜索完成后在回传前释放状态去重索引和实时预览缓冲，并就地把工作节点转换成持久化节点，避免最终结果构造再次复制整组节点字典；
- Windows 3.12 入口使用系统 `malloc` 隔离解释器分配器，不再启用高内存开销的 debug allocator；faulthandler 继续保留；
- 计算 worker 在加载模拟器前检查导入图；若 Pygame、SQLAlchemy 或 greenlet 意外进入 worker，立即以普通 Python 异常退出并由 GUI 展示，不允许带着原生模块继续执行大图搜索；
- 实际验证：模块测试 21 项通过；5 人全部站位 DFS/BFS 均为 95 状态、600 条好人路径、3600 条狼人路径；6 人复杂角色单站位完成 20281 状态；5 人完整五战术单站位完成 14928 状态；5 次独立多进程 CLI 循环全部退出 0；隔离 worker 环境下连续 10 个独立进程、每次 100000 个子节点的 interval 回传均完成。Pygame 同进程对照仍可触发原生段错误，证明进程隔离和导入硬检查是必要边界；Pygame 五帧启动/绘制 smoke 退出 0。

### 依赖与运行环境

- 继续使用父项目 Python 3.12 `.venv`；
- 已在共享环境安装 `pygame-ce==2.5.8`、`pygame_gui==0.6.14` 和 `SQLAlchemy 2.x`；
- 在 `requirements.txt` 声明模块 UI 依赖；
- 不创建第二套虚拟环境；
- 不修改父项目文档。

### 实际文件映射

- `_config.py`：DFS 默认、智能投票、愚者、lambda 和昼夜战术 CLI 参数；
- `_positions.py`：角色多重集合、愚者技能和唯一站位枚举；
- `_game_state.py`：公共声明、愚者揭示、无损紧凑状态的完整 GameState 按需重建及 API 续算字段；
- `_strategy.py`：白天/黑夜基线与战术 profile；
- `_simulator.py`：信息隔离、智能目标、愚者结算和昼夜分支；
- `_interval.py`：wide/narrow 数据结构、传播公式和着色；
- `_tree_search.py`：DFS/BFS DAG、完整边原因、路径计数与动态 interval 回传；
- `_sqlite_lru_signature_store.py`：基于 SQLAlchemy Core 的状态签名 LRU、wide/narrow、GameState 和原因 JSON 持久化；
- `_gui.py`：Pygame 分页、统计、滑块、hover 和 DAG 可视化；
- `_plotting.py`、`_reporting.py`、`_artifacts.py`：wide/narrow 输出与 wide 着色；
- `_i18n.py`：中文默认 UI 与 CLI 英文资源；
- `tests/test_search_simulator_tree.py`：新算法契约的回归测试。

### 配置层

- 搜索默认值由 BFS 改为 DFS；
- CLI 保留 `--search_mode bfs|dfs`；
- GUI 移除搜索模式控件并始终提交 DFS；
- 删除 `confidence_level`；
- 新增 `lambda_risk`，默认 `0.5`；
- 新增默认开启的 `smart_vote`；
- 新增默认关闭的 `include_idiot`；
- 战术配置拆成白天与黑夜；
- GUI 固定中文，不显示语言切换控件；
- CLI 继续保留语言参数。

### 角色与状态层

- 新增愚者角色和“身份揭示”技能；
- 状态增加愚者揭示信息；
- 状态增加公共身份声明；
- 保留公共阵营确认与预言家私有查验的区分；
- 死亡结算不自动写入公开身份；
- API 序列化和克隆覆盖所有新增字段；
- 状态签名纳入所有影响未来转移的字段；
- interval、父节点编号和纯 UI 数据不进入状态签名。
- 状态签名直接读取已构造的紧凑状态键，以显式栈写入带类型与长度分隔的小型规范缓冲，再使用标准库 BLAKE2b 生成 128 位十六进制值；禁止递归闭包、`nonlocal` 热循环、完整 `repr` 或再次构造状态键。该签名只改变编码实现，不改变参与签名的字段集合；

### 战术模型

白天战术：

- 村民挡刀；
- 预言家不跳；
- 狼人跟票。

黑夜战术：

- 狼人自刀骗解药；
- 狼人空刀。

战术树只在智能投票开启时生效。已勾选战术增加分支，正常行动基线保留。每个战术分支在目标过滤时具有最高优先级。

自刀分支要求存在女巫或守卫且存活狼人不少于两名。空刀要求存在女巫或守卫。无效战术不生成分支，UI 同步禁用不可用勾选项。

### 智能投票与狼刀

- 建立阵营识别辅助层，只允许读取行动者私有信息和公共信息；
- 投票先应用战术，再执行阵营过滤和个人启发式；
- 预言家使用私有查验，其他好人只使用公共查验；
- 狼人排除狼队成员；
- 同级目标全部展开，不使用固定座位 tie-break；
- 已揭示愚者不投票，也不成为智能投票目标；
- 狼刀优先公开神职声明者，依次为女巫、守卫、预言家、愚者；
- 守卫、女巫和预言家行动继续完整展开。

### 愚者结算

- 白天放逐愚者时不调用普通死亡链；
- 写入愚者揭示状态并消耗身份揭示技能；
- 当天直接进入夜间，不进行二次放逐；
- 愚者继续计入存活神职；
- 后续票型构造跳过其投票；
- 胜负条件将愚者计入神职集合。

### Robust interval

- 删除 Wilson 与正态分布依赖；
- interval 数据改为同时保存 wide/narrow；
- 终局分别写入 `[1,1]` 和 `[-1,-1]`；
- 搜索完成后按逆拓扑序回传；
- 每个父节点对唯一直接子节点去重；
- interval 回传忽略边 multiplicity；
- narrow 倒置时自动交换端点；
- λ 默认 `0.5`；
- 边颜色只读取子节点 wide interval；
- interval 不进入智能投票、状态签名或 DAG 合并。

### DAG 与 LRU

- 规范状态键只描述未来转移所需的 GameState；
- 只合并未来转移完全等价的状态；
- 内存 LRU 保存状态签名到节点标识的热点映射；
- SQLite 签名记录只表示游戏状态；
- 父子边保存原因列表，而不是一个代表 action；
- 每个原因单独保存 action key、可读标签和 multiplicity；
- interval 与结果摘要保存在图节点/边结果中，不进入 LRU key。

### SQLite 持久化与 schema

- 所有建表、索引、查询、写入、upsert、更新、删除和计数统一使用 SQLAlchemy Core 的元数据与表达式 API；模块源码和测试中禁止出现明文 SQL/DDL/PRAGMA；
- 当前 schema 额外包含 `lru`、`memory`、`solution` 三张职责表：`lru` 承载磁盘热签名，`memory` 承载运行/断点摘要，`solution` 只登记完整站位结果并引用既有图数据；具体增量和加载流程见 `CODE_CHANGES.md`；
- SQLite 连接、事务提交、复合主键冲突处理和 LRU 磁盘清理均通过封装 API 执行，不向搜索层或 GUI 暴露 cursor 或 SQL 字符串；
- 启动时使用 Inspector API 校验已有表的必需列；不兼容的旧缓存明确报告需要重建，不用字符串拼接执行隐式迁移；

- 图节点增加 wide/narrow 四个边界列；
- 图边增加完整原因 JSON；
- 新建 schema 只包含 wide/narrow 边界和 reasons JSON，不再创建旧 reward 列或单 action 代替结构；
- 已有库由 Inspector 对必需列做非破坏校验；兼容 schema 直接使用，不兼容 schema 拒绝启动并要求保留/移走旧缓存后重建；
- 大整数路径计数继续使用 TEXT；
- worker 继续只计算，父进程保持 SQLite 单写；
- 计算 worker 的模块导入图不得包含 SQLite、SQLAlchemy 或 greenlet；持久化类只在父进程进入站位批处理协调器后惰性导入。IPC/SQLite 公用的批次上限放在纯 Python 配置层，避免 worker 为读取常量而加载数据库原生扩展；
- GUI 进度队列容量为 32，完整结果队列容量为 8；队列满时生产者等待消费者形成背压，不允许结果消息无界积压；
- 每个站位先发送小型摘要，再将节点和边分别按最多 64 条发送，最后发送完成标记；SQLite 写线程收到批次即通过持久化 API 提交，不再等待或复制整棵 DAG；64 条上限同时约束 IPC 消息、SQLAlchemy 参数装配和 SQLite 单次 executemany 的瞬时内存；
- 任务调度最多为每个 worker 保留一个 in-flight 站位，避免进程池待执行栈与站位数同步增长；GUI 即使单 worker 也走该结果队列；
- worker 每完成一个站位即退出并由进程池补充，释放 Python/原生分配器可能保留的大图内存；worker 崩溃或数据库写入失败时，未完成站位的部分节点、边和摘要由持久化 API 清理。
- 运行请求先查询 `solution`；命中时返回摘要并按需载入 DAG，GUI 下一次点击才传递 `force_recompute` 创建新批次；该流程不复制完整图，也不把中断记录当作解。

### Pygame GUI

- 删除 Tkinter 依赖；
- 使用 Pygame 主循环和 `pygame_gui` 控件；
- 所有标签按独立布局槽位排版，不允许页码、缓存、进度或状态文本共享基线发生重叠；
- 自绘复选框、战术分组、站位行、DAG 节点和分支边提供 hover 高亮，边和节点 hover 显示完整派生原因与状态详情；
- 每个参数输入框提供 hover 文本，说明参数语义、合法范围和界面限制；
- `lambda` 使用 `[0,1]` 滑块与实时数值文本组合，不接受自由文本输入；
- 搜索完成后滑块变化对当前选中 DAG 执行无权重逆拓扑回传，实时更新节点、边、根区间与颜色，不重新运行 DFS；未选中站位在选中时按当前值惰性重算；
- 每个站位结果回调实时累计终局节点、好人路径、狼人路径和当前路径占优关系，并在 DAG 顶部独立统计条显示；
- 搜索 worker 以 0.5 秒为硬节流周期上报站位编号、已展开节点、已发现节点、frontier、边数和已发现终局数；GUI 同样每 0.5 秒批量消费进度队列，但 Pygame 输入、hover、暂停/恢复和动画保持正常帧率；
- 节点级事件附带有界的增量预览批次：站位角色、最近发现或更新的节点、最近生成的边及派生标签；预览数据不写入状态签名和 interval；
- 站位开始事件先在结果表创建“计算中”占位行，节点事件实时更新该行的状态数、边数、终局数和耗时，完成事件原位替换为正式 wide/narrow 结果；
- DAG 面板在运行中固定显示一个活跃站位的滚动局部 DAG，完成后自动切换到下一活跃站位；窗口只保留根节点与最近节点，并过滤悬空边，防止跨进程消息和绘制成本随完整图无限增长；
- 实时节点首次进入可见子树时记录本地可见时间，绘制层在短时内对节点、连线和文字颜色做透明度插值；节点后续状态覆盖保留原首见时间；
- DAG 为每个站位维护本地展开节点集合。初始只布局入度为零的局部入口；点击节点在展开/收起状态间切换，可见图仅沿展开节点向下遍历。布局、hover 命中、边标签和节点绘制只处理该可见图；
- DAG 标题区提供“全部展开 / 全部收起”按钮：展开将当前观测图内所有有子边节点加入展开集，收起清空该集合；两个操作均只更改 UI 本地状态；
- 开始按钮旁提供暂停/恢复控件；暂停信号由 GUI 主进程控制并共享给 worker，搜索循环在下一节点展开前等待，恢复后沿原 frontier 继续；
- DAG 节点只绘制紧凑编号与阶段标记；搜索内存与 SQLite `state_json` 保存无损紧凑快照，实时预览批次和 hover 才按需重建只读 GameState 字典。hover 以多行检视器完整渲染所有 GameState 字段和每名玩家字段，不再限制为摘要的固定行数；
- interval 回传不再将子节点转换为 `RobustIntervals` 列表；直接从节点的四个浮点边界流式累加 count/sum/min/max，使单个高出度节点的额外空间为常数；
- 昼夜展开不再先构造节点的完整 `StateTransition` 列表；战术 profile 内的合法动作以生成器逐条交给 DAG 合并层，单条转移完成签名和边原因归并后即可释放临时 GameState。该变化只降低单节点瞬时内存，不跳过任何 profile、目标或票型；
- GUI 只配置一个 worker 时也通过 `ProcessPoolExecutor` 执行站位搜索；Pygame 主进程只保留绘制、事件合并与 SQLite 单写。子进程崩溃通过 future 错误返回 GUI，不让原生访问冲突终止界面进程；
- CLI 的站位批处理同样始终通过隔离子进程执行，即使只有一个站位且 `parallel_workers=1`；CLI 主进程不直接进入长时间状态展开，worker 原生异常由进程池边界转换为可报告的任务失败；CLI 未提供外部结果队列时，由批处理协调器创建生命周期受控的 Manager 有界队列，搜索结果禁止退化为整棵 DAG 的 Future 返回值；
- 站位 DAG 不作为单个 Future 返回值跨进程复制；有界结果队列按摘要、节点批次、边批次、完成标记依次传递，独立写线程持续落库，使搜索内存、IPC 队列和 SQLite 批次都有固定上限；
- 每个角色、运行选项、战术分组和战术复选框提供 hover 文本，解释分支语义与禁用条件；
- 计算在线程或子进程中运行，所有绘制只在 Pygame 主线程发生；
- GUI 固定 DFS；
- 智能投票默认勾选；
- 白天/黑夜战术采用可扩展分组与复选框组合；
- 智能投票关闭时折叠并禁用战术；
- 角色变化实时更新夜间战术可用性；
- 结果表分页；
- DAG 支持平移、缩放、节点选择和边原因详情；
- 玩家 icon 区分真实身份、声明、公共确认和私有查验；
- wide/narrow 同时显示，边颜色固定按 wide；
- UI 固定中文，不显示语言选择按钮。

### i18n

- 保留文案键和值访问层；
- 中文作为 GUI 固定语言；
- 英文资源继续服务 CLI/API；
- 删除 GUI 语言切换事件和控件；
- 角色名、phase、action key 等领域机器值保持稳定。

### 输出与绘图

- JSON 同时输出 wide/narrow；
- 边输出完整原因数组；
- 站位摘要显示 wide/narrow、阵营颜色和智能模式；
- 静态图和 Pygame DAG 均使用 wide 着色；
- 已查验玩家在上帝视角 UI 使用 icon 标注。

### 验证记录

- Python：共享解释器为 `3.12.10`；
- 站位：默认七人板子枚举 1260 个唯一站位，启用愚者后枚举 2520 个；
- 测试：`python -m pytest tests/test_search_simulator_tree.py -q`，31 项通过；包含 0.5 秒节流、逐层展开/收起、淡入标记、中文分区 GameState Hover、强背压队列、稳定流式状态签名、单站位 worker 约束、显式具名参数边界、内存阈值、同运行恢复、完整检查点跳过、时间戳 crash 路径、历史崩溃提示、互斥终态弹窗与摘要、worker 崩溃 CRITICAL 日志、禁止 interval 回传生成器帧/子节点序列物化和明文 SQL 防回归检查；
- 静态：`python -m ruff check search_simulator tests/test_search_simulator_tree.py` 通过；
- 编译：`python -m compileall -q search_simulator` 通过；
- CLI：默认 DFS 与显式 `--search_mode bfs` 均完成 3 人小局，输出图等价且生成 wide/narrow；
- SQLite：测试覆盖 Core API schema 创建、Inspector 列检查、站位分页、紧凑节点/边读取、完整 GameState 按需还原、原因数组、wide/narrow，以及容量为 2 的结果队列背压写入；
- interval：测试覆盖 multiplicity 无关、lambda 动态重算、narrow 规范化、同号优先着色，以及不允许物化十万子节点序列的流式聚合；
- 战术与角色：测试覆盖自刀/空刀合法性、愚者 2520 站位、揭示免死和失票；
- GUI 无显示测试：Pygame 主循环、全部展开/全部收起控件和首帧绘制通过；模块回归另完成 3 人 DFS，1 个站位、3 个状态、2 个终局节点；
- Windows 原生崩溃复现：在 CPython 3.12.10 / pygame-ce 2.5.8 下，大图 interval 回传只要与 Pygame 原生扩展同处一个进程，即使边界计算已改为顺序比较，扩展压力样本仍可能触发 `pygame_parachute` 访问冲突；正式路径因此以进程隔离和 worker 导入硬检查为边界，不再宣称同进程计算可以通过分配器设置修复；
- Windows 高复杂度 CLI 压力进一步发现 CPython 3.12.10 在长时间搜索、区间生成器帧和大批 SQLAlchemy 参数装配路径可能随机访问冲突；模块入口在 Windows 3.12 及以上改用系统 `malloc`，不再使用高额外内存的 debug allocator。持久化批次保持 64，并在 interval 回传前释放状态索引与预览缓冲；
- 进程隔离：容量为 2 的结果队列回归覆盖单 worker 子进程、强背压、分批 SQLite 写入和完整站位摘要；CLI 入口不再为 DFS/BFS 无条件导入 Pygame；
- GUI 真实窗口：先前已确认固定 DFS、中文字体、lambda 滑块、八项统计条、无标签重叠、智能投票隐藏战术和 hover 文本；本轮移除站位进程数输入并显示“站位串行 · 自动检查点续算”，完成 Pygame 五帧无显示启动回归；
- 完整默认 1260 站位的组合数量已验证；本轮按用户要求终止优化前的高内存完整运行，优化完成后没有重新启动。

### 2026-08-23 CLI 压力栈验证

- 5 人、1 狼、无神职、5 个唯一站位：DFS 与 BFS 均退出 0，均得到 95 个状态、好人路径 600、狼人路径 3600，wide/narrow 结果完全一致；该记录最初来自旧并发实现，本轮串行检查点实现已复跑并保持相同结果；
- 最终实现连续启动 10 次上述 DFS CLI：10/10 退出 0，共调度 50 个站位任务、950 个状态，累计约 33.57 秒，无 worker 异常和父进程异常；
- 6 人、2 狼、预言家、女巫、守卫、村民，关闭战术但保留完整角色行动：单站位 DFS 退出 0，完成 20281 个状态，好人路径 12061221472、狼人路径 22545359930，耗时约 10.58 秒；
- 同一 6 人板子五类战术分别单独启用时均退出 0；完整五战术组合会生成百万级以上 DAG，不适合作为短时回归。最终实现对该组合执行超过 10 分钟持续 CLI 满载，worker 私有内存稳定约 1.53 GB、持续占用 CPU，期间无新增 Windows WER 或崩溃日志；样本由测试方按压力边界发送中断，不能记录为完整搜索完成；
- 压力过程曾依次复现 worker 搜索、整图 Future 回传、500 行 SQLAlchemy 参数批次、`repr().encode()` 状态签名和 interval 生成器帧的访问冲突。最终防线为：所有 CLI/GUI 搜索进程隔离、worker 禁止导入 Pygame/SQLAlchemy/greenlet、有界结果队列、64 行持久化批次、系统 `malloc`、搜索后释放去重索引、就地节点持久化转换、显式 interval 累加循环、惰性 StateTransition，以及非递归、无闭包的规范 128 位状态签名。

## 2026-08-22：完整 BFS/DFS 分支 DAG 重构

上一阶段已完成：

- 移除随机游走、在线策略、minimax 和旧剪枝路径；
- 建立 BFS/DFS 状态 DAG；
- 枚举默认七人板子 1260 个站位；
- 增加白天三类战术；
- 增加 SQLite 站位、节点、边和大整数路径持久化；
- 增加 GameState 续算通道；
- 增加分页结果与中英文文案基础。

该阶段的 Wilson interval、Tkinter GUI、默认 BFS 和无愚者模型将在 2026-08-23 重构中替换。
