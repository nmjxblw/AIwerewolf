# Search Simulator 代码更新记录

本文档只记录 `search_simulator/` 的工程实现事实；当前算法模型、状态转移语义和数学公式见 `ALGORITHM_DESIGN_V3.md`，精确信念 Cheap-talk 决策矩阵的工程方案与实现约束见 `CODE_DESIGN_V3.md`，有限隐藏世界精确信念与完整分支树迭代文档只作历史归档。文档文件名中的编号只表示沿革，不作为算法名称；未落地的设计不得提前写成已完成事实。

历史记录中随旧持久化数据出现的 `v2`、`v3`、`v4` 仅表示检查点或紧凑状态的存储格式。它们不是算法名称；新增代码、日志、docstring、CLI、GUI、结构化输出和验证记录均使用具体算法名称或职责名称。

## 持久化分层

- SQLite 通过 SQLAlchemy Core 元数据和表达式 API 创建、查询、写入、更新、删除及校验，不向搜索层暴露 cursor，也不在源码中使用明文 SQL。
- `lru` 表保存站位感知的状态签名热缓存。内存 `OrderedDict` 负责最近命中，SQLite `lru` 负责容量受限的磁盘回落；interval、节点边关系和 UI 字段不进入该表的签名键。
- `memory` 表保存运行批次和断点信息，包括规范配置、终态、摘要、已完成站位数、下一恢复站位和更新时间。站位节点/边数量校验通过后才写入可恢复检查点。
- `solution` 表保存完整运行的站位结果索引，包含规范解配置、来源运行 ID、站位摘要和节点/边计数。只有 `complete` 且完整性校验通过的站位才能登记；中断和失败运行不会进入该表。
- 既有 `simulation_runs`、`position_results`、`graph_nodes`、`graph_edges` 表继续承担图数据存储；`memory`/`solution` 是运行状态和解索引层，不复制整棵 DAG。
- 打开旧缓存时通过 Core 批量迁移 `state_signatures` 到 `lru`，并为旧 `simulation_runs` 补建 `memory` 行；不会执行明文迁移 SQL，也不会删除旧图数据。

## solution 优先加载

- 搜索协调器启动时先通过持久化 API 查找同一规范配置的完整 `solution`。
- 命中时返回 `loaded_solution=true` 的结果对象，载入站位摘要；图节点和边仍按 UI 选择通过持久化 API 分页/按站位读取，不在加载路径复制完整 DAG。
- GUI 首次点击运行时显示“已载入已有解”，保留结果行和 DAG 查看能力；下一次点击将显式传入 `force_recompute=true`，创建新的运行批次并重新展开。
- CLI 提供同名的显式强制重算参数，默认保持 solution 优先加载。没有已有 solution 时，默认请求直接进入正常 DFS/BFS 搜索和 `memory` 断点流程。
- 重新计算完成后，新的完整站位摘要写入 `solution`，并保留旧运行记录以支持历史审计。

## 参数与边界

- `force_recompute` 从 CLI/GUI 入口、模拟器配置到站位批处理均使用具名字段显式传递。
- `lambda_risk` 仍只用于搜索后的 interval 观测；solution 配置键在比较时排除该字段，使同一 DAG 可以动态观察不同 lambda。
- 读取已有 solution 不改变状态签名、DAG 合并、路径计数和 interval 传播；重新计算才会创建新的搜索任务和新的检查点。

## Windows 原生崩溃隔离与站位内恢复

- Windows 物理内存读取改为模块级静态 `MEMORYSTATUSEX` ABI 与显式 `argtypes`/`restype` 绑定，避免在内存检查热路径反复创建 ctypes 类型和使用隐式调用签名。
- 计算 worker 以 `-S` 隔离启动，启动后校验不得加载 Pygame、SQLAlchemy 或 greenlet；父进程仍使用项目共享的 Python 3.12 `.venv` 和完整 site 环境。
- worker 常驻状态键升级为可逆的单块二进制编码，持久化/API 边界再恢复为扁平数组；状态签名延迟到批量落库时生成。旧版 v2 站位内检查点在恢复时原地迁移为 v3，不同时保留新旧两份去重索引。
- 邻接拓扑改用连续整数数组保存父节点、子节点和 multiplicity；派生原因只在当前未落库分块暂存，分块边通过有界结果队列交给唯一 SQLite 写入者。
- 每个 worker 只处理一个站位分块，随后退出。其检查点对象没有引用环，因此 worker 内关闭循环 GC，普通引用计数仍正常工作；进程退出回收全部剩余对象。此隔离仅作用于计算子进程，不修改 GUI/父进程 GC。
- 系统可用内存由父协调器在每个短分块派发前后检查；worker 通过显式任务参数禁止调用系统物理内存 ctypes API，避免大型 Python 堆展开与 Win32 ABI 同时处于热路径。内存安全保留线仍生效，最坏延迟为一个有界分块。
- 每个分块使用独立进程池。若子进程发生 access violation、`BrokenProcessPool` 或解释器对象槽位错位，协调器保留上一个原子检查点、缩小节点预算并自动重试；连续失败超过上限才把运行写为 `failed`。成功检查点会把实际预算写入 `memory`，应用重启后沿用该预算，避免重复触发已知危险窗口。
- 站位内 checkpoint 和完整站位 checkpoint 使用不同完成语义。暂存摘要的节点/边计数使用 `-1` 哨兵；只有实际节点数大于零，且节点/边实际计数分别等于最终摘要时，站位才能被跳过或登记到 `solution`。
- `complete` 终态增加二次防线：摘要必须覆盖全部预期站位，每个站位至少存在根节点，否则持久化 API 拒绝登记 solution。历史上由 `0 == 0` 暂存计数造成的空图不会再被识别为已有解。

## 本次压力验证结论

- 原始 access violation 的栈帧曾落在状态克隆、区间回传、昼夜分支和状态编码等不同纯 Python 行；它不是可复现于某一条游戏规则的确定性异常。
- 单独对状态克隆、签名和 Windows 内存 ABI 进行百万级压力均可通过；默认 7 人单站位搜索则显示，风险随恢复检查点中的 Python 容器数量增长而上升。
- 同一 `run_id` 从 150,000 节点的旧检查点恢复，v3 迁移后先越过此前崩溃点并写到 225,000；协调器自动重试、二进制状态键、GC 隔离和 worker ABI 隔离共同把可恢复检查点推进到已处理 593,750、已发现 595,813、边 3,019,460、frontier 2,063。
- 该压力运行已显式写为 `interrupted` 并保留站位内检查点，没有登记为 solution。完整 pickle 图在规模继续增长时仍会迫使 worker 缩小分块预算，因此当前实现提供可靠的崩溃恢复保底，但尚不能把“整图反序列化”的规模风险表述为已经消除。

## 验证记录

2026-08-23 实际验证：

- 使用项目共享 Python 3.12 `.venv` 执行 `pytest tests/test_search_simulator_tree.py -q`：45 项通过；包含站位内恢复、暂存空图拒绝、完整 solution 防线、Core/Inspector schema、结果队列背压和无明文 SQL 回归。
- 修改模块的 `py_compile` 通过。
- 3 人 1 狼最小局 DFS CLI 完成 1/1 站位；同一 SQLite 第二次运行输出 `SOLUTION_LOADED` 且没有创建 worker。
- 3 人 1 狼显式 BFS CLI 完成 1/1 站位，结果与 DFS 的终局 interval 一致。
- Pygame 使用 dummy 视频/音频驱动启动后主循环保持存活 4 秒，由验证脚本主动停止。
- 本轮验证生成的 `crash_log/` 按约束保留。递归删除隔离验证目录与 pytest 临时目录的命令已被当前执行环境策略拒绝，未伪报清理成功。

2026-08-23 GUI 默认模式压力观察：

- 通过 Pygame GUI 保持默认 7 人板子、智能投票和战术勾选，仅取消“所有站位”，固定 DFS 单站位运行。
- 运行约 142 秒后由 UI 暂停，现场计数为已展开 327,909、已发现 330,249、边 1,636,573、终局节点 186,687、frontier 2,340；暂停状态保留可恢复断点，未误报为完成。
- 视觉检查发现旧 `_layout_graph` 将搜索深度映射到纵轴、同层节点映射到横轴，与研究 UI 约定相反；已改为深度 X 轴递增、同层节点按稳定 `node_id` 从上到下排列，并补充中文不变量注释。
- 重启修正版 GUI 后再次取消“所有站位”运行 DFS，约 246 秒时暂停，已展开 379,985、已发现 382,121、边 1,856,622、终局节点 218,542；实时观测窗口出现多层节点列，视觉上确认深度向右、同层向下，未出现新的原生崩溃。
- 使用项目 `.venv` 对 `_layout_graph` 做静态坐标回归：深度 0/1/2 分别得到 X=0/105/210，同层两个节点得到 Y=-72.5/72.5；`py_compile _gui.py` 通过。
- DAG 预览横向基准改为画布左内边距（不再以画布中心为根层基准），因此根节点左对齐、后续深度向右展开，同时保留平移和缩放；新增中文注释说明该坐标不变量。
- DAG 边的派生原因标签改为仅在悬停折线时显示，常态只保留分支线；悬停提示增加完整原因标题与全部原因行，取消实时预览对原因数量和文本长度的截断。
- 本次验证：45 项模块测试通过，`ruff`、`py_compile` 和 Pygame dummy 3 帧 GUI 启动通过；边 hover 文本统一走换行/多列路径，不再受短提示的前 7 行限制。
- DAG 控件区新增“定位根节点”按钮；点击只重置本地画布平移并选中稳定的最小入口节点，不改变展开集合、frontier 或持久化图。按钮、说明文字和状态提示均纳入中英文 i18n。
- 根节点定位回归测试加入后，模块测试为 45 项通过；ruff、py_compile、Pygame dummy 3 帧启动和 `git diff --check` 均通过。

## 2026-08-23 文档整理与 GUI 视觉验收

- 早期 `ALGORITHM_DESIGN.md` 重新收敛为纯算法文档；当前算法内容由完整分支树迭代、有限隐藏世界精确信念历史归档和 `ALGORITHM_DESIGN_V3.md` 分层承接，保留零和建模、信息隔离、状态等价、战术分支、终局效用和 wide/narrow 公式，工程清单移至代码文档。
- 早期 `strategy_implemetation.md` 的工程补充现由 `STRATEGY_IMPLEMENTATION.md` 承接，包含同级目标不得用票数向量裁决、隔离 worker/有界队列、运行终态/crash 证据、实时观测窗口和清理纪律；边原因明确为“折线 hover 显示、常态隐藏”。
- `CODE_DESIGN.md` 增加历史文档声明：当前工程事实、测试数量和最新验证以本文件为准，旧日期记录不覆盖增量记录。
- 使用默认 7 人板子、智能投票和战术勾选，取消“所有站位”后启动固定 DFS 单站位视觉检测；运行期间实时显示展开/发现/frontier/边/终局计数，随后主动暂停，现场未将未完成站位误报为 complete。
- 视觉上确认 DAG 深度从左到右、同层节点从上到下；“定位根节点”能把根节点放到缩略图左侧并选中；“全部展开/全部收起”只改变当前观测窗口；节点 hover 使用中文分区文本显示完整 GameState。
- 视觉检测发现实时 DAG 边密集时单条边 hover 不易稳定命中；代码路径已保留完整原因多行渲染，但该 hit-test 仍列为下一轮 GUI 回归的显式检查项，不能仅凭节点详情栏替代边 hover 通过证据。
- 本轮只产生文档改动；临时 GUI 进程已关闭，未新增或提交测试数据库、JSON、截图和日志。

## 2026-08-23 DAG 网格深度坐标布局

- `_gui.py` 的 DAG 坐标改为固定网格：节点显式迭代深度优先映射到 X 轴列，旧持久化节点回退读取紧凑观测中的深度，再退化为昼夜轮次之和；同一深度按稳定 `node_id` 占用固定 Y 轴网格行。
- DAG 视口新增横向“迭代深度 X”坐标轴、`D0`/`D1` 深度刻度、竖向虚线深度分隔和有限范围的横向网格线；网格绘制受当前视口和缩放限制，不改变状态、边或 interval。
- 轴线和刻度为节点绘制预留底部空间，根节点定位、平移、缩放、点击展开/收起和 hover 命中继续使用同一画布坐标变换。
- 新增固定列/行坐标回归测试；共享 Python 3.12 `.venv` 下模块测试为 46 项通过，`ruff`、`py_compile` 和 Pygame dummy 图形绘制 smoke 均通过。

## 2026-08-23 Computer Use GUI 视觉验收规范

- 将 GUI 视觉验收要求固化到 `AGENTS.md`：任何 GUI 代码、布局、样式、交互、hover、动画或可视化改动，都必须用 Computer Use（电脑工具）启动真实 Windows 窗口检查；离屏 smoke、截图或坐标回归只能作为补充，不能替代真实窗口验收。
- 本轮使用电脑工具启动真实 `狼人杀树分支迭代模拟器`，确认中文固定 DFS 界面、lambda 滑块与数值显示、参数 hover 文本、角色勾选与战术禁用提示、单站位模式，以及开始迭代后的运行状态和完成弹窗均可见且未发现明显重叠。
- 以 3 人 1 狼、无神职、关闭“全部站位”的最小配置完成 1/1 站位 DFS；完成弹窗显示运行 ID、检查点、日志路径和 crash 日志路径，结果表显示节点/边/终局计数。该次真实窗口检查未提交截图、临时数据库或日志文件。

## 2026-08-23 access violation 检查点与绘制路径修复

- 检查点格式升级为 v4：节点快照、frontier、父子节点数组、multiplicity 数组和派生原因分块写入独立 pickle 记录；节点块为 2048 条、原因块为 1024 条。写入临时文件后执行 flush/fsync，再原子替换正式检查点，避免一次性 `pickle.dump` 整棵大图造成瞬时序列化峰值。
- v4 不持久化可由节点快照重建的 `node_id_by_key` 重复索引；加载时校验 magic、版本、规范配置、计数和重复状态键。既有 v2/v3 检查点仍可读取，恢复后由下一次保存迁移为 v4。
- GUI 图形坐标统一经过有限值检查和边界裁剪。水平/垂直线使用安全整数填充，虚线先在小型透明离屏 surface 上合成后单次 blit 到真实窗口；DAG 边也使用安全绘制路径，避免把浮点或极端坐标直接交给 Windows Pygame-ce 原生绘制函数。
- 节点详情读取紧凑状态快照时改为统一的状态恢复函数，并对损坏或旧格式快照做受控异常处理，避免详情面板因紧凑元组下标不匹配引发绘制线程异常。

### 实际验证

- 使用 Computer Use 启动真实 Windows 窗口 `狼人杀树分支迭代模拟器`：启动画面稳定，中文固定 DFS 界面、深度轴、竖向深度分隔线和横向虚线网格可见，未出现标签重叠。
- 通过真实窗口设置 3 人、1 狼、关闭预言家/女巫/守卫和“全部站位”，保持智能投票；点击开始后立即显示 worker 启动中的运行状态和可用暂停按钮，随后完成 1/1 站位（3 个节点、2 条边、100%）。
- 通过真实窗口点击结果行加载持久化 DAG，节点详情栏显示中文玩家信息及完整边原因；本次验收期间未再次出现 access violation。验收结束后已关闭 GUI，并清理本轮遗留的孤立 Python worker helper 进程。
- 共享 Python 3.12 `.venv` 下 `pytest tests/test_search_simulator_tree.py -q` 为 46 项通过；`py_compile`、`ruff check` 和 `git diff --check` 通过。
- 独立 v4 检查点往返验证通过（分块读取、10 个节点、12 条边）；最小配置 DFS CLI 和显式 BFS CLI 均完成 1/1 站位；dummy Pygame 安全绘制 smoke 覆盖有限值、极端坐标和非有限坐标，真实 display 的裁剪/填充 smoke 通过。

## 2026-08-23 暂停迭代树 GUI 渲染

- `_gui.py` 增加临时开关 `ENABLE_ITERATION_TREE_RENDERING = False`。GUI 不再进入实时预览节点合并、SQLite DAG 加载、布局、节点/边绘制、hover、平移缩放和 lambda interval 重算路径；“全部展开/收起/定位根节点”控件同步隐藏。
- 后台进度队列仍由 GUI 持续消费，站位进度、结果表、统计卡片、进度条、暂停/恢复、检查点续算和终态弹窗保持可用。树区域改为明确提示面板，不保留旧的空白或假进度展示。
- 使用 Computer Use 启动真实 Windows GUI，设置 3 人、1 狼，关闭预言家/女巫/守卫和“全部站位”，先验证已有 solution 加载，再点击“重新迭代 · DFS”强制启动 worker。worker 运行中立即显示“迭代中 · DFS”和可用暂停按钮，最终完成 1/1 站位；结果表为 3 个状态、2 条边、2 个终局，树统计不触发 DAG 绘制。
- 完成弹窗显示 `complete`、运行 ID、检查点 `1/1`、无下一恢复站位和日志路径；本次运行对应 crash 日志大小为 0，验收期间未再次出现 access violation。窗口已通过真实 GUI 操作关闭。
- 本次代码验证：`py_compile _gui.py`、`ruff check _gui.py`、`pytest tests/test_search_simulator_tree.py -q`（46 项通过）和 `git diff --check` 均通过。该开关为临时 GUI 降级措施，后续恢复树 UI 时需重新执行真实窗口视觉验收。

## 2026-08-24 worker 检查点边原因流式修复

- 根据两条 worker 都落在 `_save_search_checkpoint` 边原因 pickle 行的崩溃栈，确认计算 worker 构造 `SearchSimulator` 时遗漏了 `result_queue`。因此 `_search_root` 的 `stream_staged_edges` 一直为假，边原因持续积累到 checkpoint 保存阶段，最终在大批量 `pending_reasons` 序列化时触发 Windows 原生 access violation。
- `_position_task` 现在显式把 `result_queue` 传入 worker 模拟器；检查点边在保存前通过有界结果队列分批交给父进程并从 worker 暂存中释放，checkpoint 不再重复持有已写入边的原因对象。
- 从站位内检查点恢复时，父进程先重建/清空暂存站位；worker 将已有边从游标 0 重新流式回放，兼容此前未流式保存的旧检查点，避免恢复后再次把历史原因聚合进 pickle。
- 新增检查点回归测试，验证边原因先进入 `position_stage_edges`，且保存后的 checkpoint `pending_reasons` 为空。

### 实际验证

- `pytest tests/test_search_simulator_tree.py -q`：47 项通过。
- 3 人、1 狼、无神职、单站位强制 DFS：1/1 完成，3 个状态、2 条边、2 个终局。
- 同一 SQLite 第二次 DFS：输出 `SOLUTION_LOADED`，未创建新的 worker。
- 同配置显式 BFS：1/1 完成，3 个状态、2 条边、2 个终局。
- `py_compile _tree_search.py`、`ruff check _tree_search.py` 和 `git diff --check` 通过；隔离 CLI 输出目录已清理，正式 `crash_log/` 按约束保留。

## 2026-08-24 关闭 GUI 实时预览负载

- 根据 `BrokenProcessPool` 后续 crash 日志确认：GUI 树渲染已经关闭，但 worker 仍在构造完整节点快照、边原因和实时预览事件；这会额外占用 worker 堆、跨进程队列和父进程内存，增加状态编码与 SQLite 写入并发压力。
- `SearchSimulator` 增加显式 `live_preview_enabled` 边界参数。GUI 固定传 `False`；worker 在该开关关闭时只发布已展开/已发现/frontier/边/终局计数和 path/interval 后处理事件，不构造或传输 `preview_nodes`、`preview_edges`。CLI/API 未显式关闭时保持原有默认值 `True`。
- 该改动不改变 DFS/BFS 分支、状态签名、检查点格式、结果队列背压和 SQLite 持久化语义；关闭的是观测负载，不是后台迭代。
- 新增回归测试，验证关闭实时预览时仍有进度事件且不会调用紧凑状态到完整 GameState 的预览重建函数。

### 实际验证

- 共享 Python 3.12 `.venv`：`pytest tests/test_search_simulator_tree.py -q`，48 项通过。
- `py_compile`、`ruff check`（`_tree_search.py`、`_simulator.py`、`__main__.py`、`_gui.py` 及测试）和 `git diff --check` 通过。
- 3 人、1 狼、无神职单站位：DFS 完成 1/1；同一 SQLite 第二次命中 `SOLUTION_LOADED`；显式 BFS 强制重算完成 1/1，三次退出码均为 0。
- 使用 Computer Use 启动真实 Windows GUI，确认崩溃提示可见且可关闭；关闭提示后真实窗口显示“迭代树渲染已暂时关闭”，后台 DFS/检查点/结果统计说明可见，未进入 DAG 绘制路径。验收窗口随后已关闭。
- 随后在真实 GUI 设置 3 人、1 狼、无预言家/女巫/守卫、单站位，第一次点击命中已有 solution；第二次点击“重新迭代 · DFS”确实进入“正在计算 1/1 个站位”，完成弹窗显示 1/1、3 状态、2 边、2 终局。该启动对应 crash 日志大小为 0，未再次出现 access violation。
- 用户提供的 `run_id=6f640f206c4c4b269b45adec27fd37a4` 仍保留在正式日志和 crash 日志中作为故障证据；该运行的 `BrokenProcessPool` 重试曾从 70,000 推进到 90,000 个已处理状态，但最终未形成终态，不能将其记为完成。

## 2026-08-27 精确信念 Cheap-talk 决策矩阵落地

### 实际代码改动

- 新增 `_decision_state.py`：提供不含完整隐藏站位的 `DecisionState`、worker 内部 `WorldState`、规范板子配置和固定终局优先级。
- 新增 `_role_view.py`：从真实世界构造行动者白名单视角，精确枚举相容隐藏站位，直接从离散 posterior 抽样，并提供 posterior 摘要与策略热路径边缘狼人评分。
- 新增 `_speech_action.py`：实现 baseline、指控、支持、投票意向、跳预言家和主动沉默六类动作族；完整跳预言家必须带目标与阵营结果，真预言家没有查验时不生成完整伪查验。
- 新增 `_rule_kernel.py`：实现精确信念 Cheap-talk 决策矩阵的单动作转移、逐席位发言、投票、夜间守护/女巫/预言家行动、均匀平票和终局判断；新增完整分支树兼容适配器保留旧 `SearchSimulator.expand_state()` 入口，不删除既有 DAG、LRU、solution 或 interval。
- 新增 `_rollout_policy.py`、`_decision_matrix_worker.py` 和 `_decision_matrix.py`：实现固定五档 policy 分数、温度 `0.25`、可信度 `0/0.5/0.8`、共同随机数、充分统计量、配对 baseline 差值、有限队列的双进程计算和单写线程聚合。
- 新增 `_decision_matrix_store.py`：在原 SQLite 文件中创建 `decision_matrix_runs`、`decision_matrix_rows`、`decision_matrix_batches` 三张职责独立的表，按请求摘要、动作键和可信度查询，批次提交幂等。
- 更新 `__init__.py`、`__main__.py` 和 `_config.py`：增加 `--decision_matrix` CLI 入口及矩阵路径、行动者、站位、worker、批次和样本参数；默认每个动作/可信度 100 条轨迹。
- 复核“同一存储数据库”要求后，将 CLI 与 `run_default_matrix()` 的默认路径修正为完整分支树迭代使用的 `search_simulator_cache.sqlite3`；仍允许显式指定同一 SQLite 文件或隔离验证库，决策矩阵只在其中新增三张矩阵表。
- CLI 输出核对发现 Windows 默认代码页会破坏 `ensure_ascii=False` 的中文 JSON；矩阵入口现显式将标准输出重配置为 UTF-8，保证重定向文件与管道可直接解析。
- 补充只读单元格查询：先用完整请求摘要定位 `complete` 运行，再用 `matrix_id + action_key + credibility` 读取单行，不启动新计算也不加载整张矩阵。
- JSON 结果补充 `baseline_delta_standard_error`，由已有配对差平方和充分统计量派生，不改变旧字段或数据库结构。
- 强制重算索引修正：查询基础请求摘要时同时匹配 `:force:<matrix_id>` 历史记录，并按更新时间选择最新 `complete` 运行，避免重算后缓存/单元格查询落空。

### 设计难点与代码修正

- 初版 worker payload 使用了 `state/actions` 短字段，而规范请求使用 `decision_state/candidate_actions`；已统一为具名规范字段，并增加静态编译和双进程 smoke。
- 夜间女巫需要在狼队投票后看到共同刀口；在 `WorldState` 增加 `pending_wolf_target`，由显式均匀随机源确定后再进入守卫/女巫/预言家顺序。
- 默认第一天行动者是预言家，真预言家必须拥有上一夜私有查验；默认请求只把第一条查验写入 `RoleView`，不写入公开状态，避免生成违规完整假查验。
- 角色视角复核时发现 rollout 的边缘评分未吸收行动者自己的私有查验，已改为把查验结果作为硬知识并阻止公开声明覆盖；同时修正公开身份揭示在目标死亡后仍应保留为硬知识。
- 规则内核补充发言目标的存活/非自指校验，以及真预言家完整查验必须来自已有私有查验且结果一致的校验。
- 请求边界收紧为冻结的三个可信度档位 `(0.0, 0.5, 0.8)`；此前可传入任意排序档位会造成与实验口径不一致，现由请求校验直接拒绝。
- 同步固定 `policy_temperature=0.25` 请求校验；避免请求身份记录非默认温度而 worker 仍按固定温度模拟的键值/行为分离。
- 可信度参数已从矩阵批次一路传入 rollout policy；隐藏世界仍使用完整 posterior 抽样，policy 目标排序使用单独版本化的硬知识/公开证据边缘评分。
- 大量重复构造动作键时遇到 Windows CPython 3.13 `json.dumps` 热路径异常；动作 `canonical()` 字段顺序固定后改为标量 tuple 的稳定 `repr` 键，避免 JSON C 加速器问题且不引入自然语言。
- 聚合线程增加有界超时和 worker 非正常退出检测；SQLite 写线程异常会回传父线程并将运行标记为 `failed`，重复 batch 只保留一次累加。
- 父进程补上既有物理内存守卫：单 worker 在每个批次前检查，多 worker 在派发前检查；进入安全区时停止派发、等待已派发批次落库并标记 `interrupted`，下次按未提交样本索引恢复。
- Windows `spawn` 导入图复核发现包级 eager 导出会提前加载 SQLAlchemy；决策矩阵相关导出在 `__init__` 改为惰性加载，父进程 API 不变，计算 worker 启动时不再触碰数据库层。

### 实际验证

- 模块目录全部 27 个 Python 文件使用项目 `.venv`（Python 3.12）执行 `py_compile` 通过。
- 既有完整分支树回归测试 `tests/test_search_simulator_tree.py` 使用项目 `.venv` 执行，48 项全部通过；惰性导出没有改变旧搜索器行为。
- 决策矩阵新增模块与入口文件通过项目 `.venv` 的 `ruff check`；10 个新增/入口相关文件完成 Ruff 格式化检查（保留旧入口中的既有格式片段）。
- 默认 7 人板子、站位 1、行动者 0（预言家）、`workers=2`、批次 10、每单元 100 样本从空 SQLite 重新运行：`status=complete`，22 个二级动作、3 个可信度档位共 66 个单元；每单元样本数均为 100，六类情景计数均闭合为 100，收益均值范围 `[-0.58, -0.34]`（在吸收真预言家私有查验的策略评分修正后重新核对）。
- `workers=1/2/4` 在相同请求、3 样本核对中得到逐 action row 完全一致的输出；证明样本索引种子不依赖进程数和完成顺序。
- CLI 标准输出契约核对通过：2 样本、单 worker 的 JSON 可按 UTF-8 直接解析，包含 22 个 action rows、可信度 `0/0.5/0.8` 和统一样本数 2。
- 内存守卫恢复核对通过：将保留比例设为 `1.0` 强制进入安全区后运行标记为 `interrupted`；恢复为正常阈值并复用同一库后按未提交批次完成 66 个单元、每单元样本数 2。
- worker 隔离回归通过：直接导入 `_decision_matrix_worker` 时 `sqlalchemy`、`pygame`、`greenlet` 均未进入模块表；惰性导出后双进程 spawn smoke 仍完成 66 个单元、每单元样本数 2。
- 角色视角隔离核对通过：狼人只获得狼队阵营知识，posterior 枚举为 60 个相容站位；守卫自守和女巫自毒均被规则内核拒绝。
- 小型精确终局 oracle 核对通过：3 人（1 狼、2 村民）仅剩行动者存活时，baseline 的三档 Monte Carlo 均值均为 `1.0`、标准误为 `0.0`，与确定性终局期望逐值一致。
- 同库幂等核对通过：3 样本运行后第二次请求命中相同 `matrix_id`，强制重算生成新 `matrix_id`；Inspector 读取到三张决策矩阵表。
- 完整分支树兼容适配器在 3 人 1 狼最小状态上返回原有 2 条 `StateTransition`，旧搜索入口仍可用。
- 所有本轮临时 SQLite、JSON 和错误输出均在读取验证结果后删除；CLI 运行产生的正式 `crash_log/` 证据按既有纪律保留。未执行 `git push`。

## 2026-08-27 决策矩阵完成后真实 GUI 视觉验收

- 通过 Computer Use 启动真实 Windows 窗口 `狼人杀树分支迭代模拟器`，确认启动时能识别并展示上一轮原生崩溃提示；提示包含 crash 日志与本次运行日志路径，点击“关闭”后正常回到主界面。
- 主配置页视觉检查通过：中文固定 DFS 标题、7 人/2 狼/每行 10、lambda `0.50`、角色与战术勾选区、站位结果表和“迭代树渲染已暂时关闭”说明均可见；左右面板和底部按钮未发现明显标签重叠或裁剪。
- 点击“开始迭代 · DFS”后约 1.5 秒捕获运行态：结果表显示站位 `1/1` 正在计算、状态 `10,813`、边 `24,668`、终局 `5,210`，下方统计卡片同步显示展开/发现/frontier/边/终局增量，按钮切换为可用“暂停”。
- 点击“暂停”后捕获暂停态：界面显示“已暂停：当前已展开 20,000 个节点”，结果计数保持、主按钮变为“已暂停 · DFS”，恢复按钮及其悬浮说明（保留 frontier、DAG 和节点计数，恢复后继续）可见且未遮挡关键区域。
- 验收结束后通过真实窗口关闭按钮退出；再次查询窗口列表未发现残留的 `狼人杀树分支迭代模拟器` 窗口。本次只做 GUI 视觉验收，未修改 GUI/算法/数据库代码，也未执行 `git push`。

## 2026-08-27 十例随机站位矩阵测试报告

- 新增 `DECISION_MATRIX_TEST_REPORT.md`，以固定 7 人板子和 `random.Random(20260827)` 生成的 10 个随机站位为输入，行动者固定为 0 号席位。
- 为避免把相同可见视角误认为不同输入，随机序列按 `request_digest` 去重后选取 10 例；报告同时列出测试审计用完整站位、行动者合法视角摘要、动作行数和请求摘要。
- 每例使用 `workers=2`、`batch_size=5`、每个具体动作×可信度 10 条 Monte Carlo 轨迹，可信度档位为 `0/0.5/0.8`，策略温度为 `0.25`；10 例全部 `complete`，共 308 个动作行、924 个动作×可信度单元。
- 报告逐例给出 matrix_id、baseline 与最优动作对比，以及全部 action rows 的均值、标准误、配对 baseline 差值和六类情景计数；所有单元样本数为 10，情景计数逐单元闭合为 10。
- 根据复核意见，报告新增“输入与输出的逐例文字对比”和“综合文本总结”：逐例解释行动者角色/硬知识、可信度变化、目标选择与 baseline 差异，并明确标准误、并列最优和小样本的解释边界。
- 测试使用独立临时 SQLite，读取结果并生成报告后已清理；报告中的 matrix_id/request_digest 仅作为审计记录。该报告样本预算为 10，生产默认 100 样本需通过新的规范请求重新计算。

## 2026-08-27 Mermaid 流程图文档规范

- `AGENTS.md` 新增文档绘图规则：流程、状态机、依赖拓扑和分支关系统一使用 Mermaid，不再用 `text` 代码块、ASCII 框线或字符箭头模拟流程图；命令、复合键、阶段枚举和非图形化示例仍保留普通代码块。
- 将 `ALGORITHM_DESIGN_V1.md`、`ALGORITHM_DESIGN_V3.md`、`CODE_DESIGN_V3.md` 和 `STRATEGY_IMPLEMENTATION.md` 中共 12 个文本流程图转换为 Mermaid，覆盖优先级链、总体架构、昼夜微阶段、阶段策略、并发拓扑、矩阵查询路径和运行状态机；未改变算法、接口、表结构或验证结论。
- 静态检查确认 12 个 Mermaid 围栏均闭合、声明类型合法且引号成对；剩余 4 个 `text` 围栏仅表达随机种子或复合键组成，不属于流程图。`git diff --check` 通过。

## 2026-08-28 GUI 决策矩阵页面与页面切换

### 实际代码改动

- `_gui.py` 在固定窗口左上角增加“树迭代 / 决策矩阵”两个页面按钮；切换页面只改变可见控件和本地观测状态，不改变完整分支树或决策矩阵请求身份。既有树迭代页继续使用固定遍历实现，矩阵计算页不展示或复用完整分支树 DAG、interval 与路径计数。
- 决策矩阵页增加默认七人板子的站位编号、行动者席位、每单元样本、计算进程、批次大小、共用 SQLite 路径和强制重算输入；GUI 使用 1 基席位，提交前转换成决策矩阵内部 0 基席位。
- 结果区按具体二级动作分页展示可信度 `0 / 0.5 / 0.8` 的收益均值与配对 baseline 差值；点击行后显示规范动作字段、Monte Carlo 标准误、配对差标准误、样本数和六类互斥情景计数。运行中只显示已提交批次数，不把中间样本均值当成完整排序。
- 新增 `_decision_matrix_gui_runner.py` 作为非 daemon 的隔离协调子进程入口。Pygame 主进程不直接执行 CPU rollout；协调进程可继续创建默认两个 rollout 子进程，并通过有界队列向 GUI 发送启动、已提交进度和三种终态消息。
- `_decision_matrix.py` 增加不改变请求摘要的进度回调与停止事件。进度只在 SQLite 批次事务成功提交后发布；停止请求只在批次边界生效，已提交结果保留为可恢复断点。
- GUI 增加“开始矩阵计算 / 中断保存”、运行进度条、矩阵 ID、已提交批次、缓存来源和 complete/interrupted/failed 三种独立模态提示；运行日志和 crash 日志路径随终态展示。
- `_i18n.py` 更新窗口标题为“狼人杀搜索与决策矩阵模拟器”，保留英文标题能力；新增矩阵 GUI 回归测试 `tests/test_search_simulator_matrix_gui.py`。

### 设计难点与修正

- 若直接把矩阵协调器设为 daemon，它无法继续创建 rollout worker；因此 GUI 只持有一个非 daemon 协调进程，由该进程管理双 worker 和 SQLite 单写线程，Pygame 仍不承载计算。
- 进度事件若在事务提交前发送，会出现界面已前进但数据库不能恢复的假检查点；现以幂等批次提交成功为唯一进度边界，缓存命中则直接报告完整批次。
- 完整分支树迭代与决策矩阵同时写同一 SQLite 会增加竞争并混淆按钮状态；两个页面允许随时切换查看，但同一 GUI 实例内禁止并发启动两类计算。
- 完整结果跨进程传输前转成 JSON-safe 结构；worker 仍只传充分统计量，不传完整轨迹、隐藏世界或 DAG。
- 非法 Windows SQLite 路径、协调进程异常退出和无终态退出均归为 failed，写入 crash 证据并使用失败弹窗，不复用“完成”文案。

### 实际验证

- 项目 `.venv` 下执行 `pytest tests/test_search_simulator_tree.py tests/test_search_simulator_matrix_gui.py -q`：51 项通过；新增测试覆盖提交进度单调、缓存命中、停止事件形成 interrupted，以及 GUI 文案不泄露上帝视角站位。
- 模块全部 Python 文件执行 `py_compile` 通过；本轮相关文件执行 `ruff check` 通过，新增 runner 与测试通过 Ruff 格式化检查。
- 隔离 Pygame dummy smoke 使用 1 样本、1 worker、批次 1 完成矩阵计算，输出 `22` 个具体动作和 `3/3` 个已提交批次。
- 完整分支树最小 3 人板子的两种 CLI 遍历均完成 3 状态、2 边、2 终局，其中第二次运行命中 solution；决策矩阵 CLI 使用 1 样本、2 worker、批次 1 完成 22 个动作和 3 个可信度档位。
- 使用 Computer Use 启动真实 Windows 窗口，确认“树迭代 / 决策矩阵”双页切换正常；两页的左右面板、页签、底部按钮与表格没有明显重叠或裁剪，SQLite 输入框和动作行 hover 均显示可见说明。
- 在真实矩阵页以默认七人板子、站位 1、行动者席位 1、1 样本、2 个计算进程运行：完成弹窗显示 matrix ID 与 `3/3` 批次，表格显示 22 个具体动作和三档独立结果；点击“主动沉默”后详情标题与动作族联动，分页从第 1 页切换到第 2 页正常。
- 对相同请求再次点击计算，真实窗口显示“已载入完整缓存矩阵”，左侧来源更新为“缓存命中”；另以非法 Windows SQLite 路径验证 failed 弹窗能显示异常、运行日志与 crash 日志路径。
- 验收结束后通过真实窗口关闭按钮退出，再次查询窗口列表无残留 GUI；临时 SQLite 与运行日志目录已清理。未执行 `git push`。

## 2026-08-28 GUI 固定共用数据库路径

### 实际代码改动

- 决策矩阵页删除 SQLite 路径输入框、标签和 hover 说明，不再允许研究者在 GUI 中把决策矩阵指向其他数据库。
- GUI 初始化时将完整分支树迭代的 `signature_cache_db_path` 解析为会话内只读绝对路径；矩阵协调进程始终使用该值。默认仍为 `search_simulator_cache.sqlite3`，两类结果保持同文件、不同职责表。
- 左栏原路径输入区改为固定存储说明，并重新排列强制重算、固定算法契约和运行状态区域。
- CLI 独立矩阵入口的显式数据库参数保持不变，继续用于隔离测试；本次限制只作用于 GUI。
- 新增 GUI 请求构造回归，验证可编辑输入集合不含 `database_path`，且协调进程参数只能取得初始化时保存的固定路径。

### 实际验证

- 项目 `.venv` 下执行 `pytest tests/test_search_simulator_tree.py tests/test_search_simulator_matrix_gui.py -q`：52 项通过。
- `_gui.py` 与矩阵 GUI 测试执行 `ruff check` 通过；`_gui.py` 执行 `py_compile` 通过；`git diff --check` 通过。
- 使用 Computer Use 启动真实 Windows 窗口并切换到“决策矩阵”页：左栏只保留站位、行动者、样本、进程和批次五个输入，原 SQLite 路径输入框不存在；静态固定存储说明可见，强制重算、算法契约和运行状态没有重叠或裁剪。
- 真实窗口使用默认七人板子、站位 1、行动者席位 1、1 样本、2 个计算进程完成固定路径计算；完成弹窗显示 `3/3` 批次，结果表显示 22 个具体动作和三档可信度。隔离验收目录中只生成固定文件名 `search_simulator_cache.sqlite3`，证明 GUI 请求未依赖可编辑路径。
- 验收窗口关闭后再次查询窗口列表无残留 GUI；隔离 SQLite 与运行日志目录随后清理。未执行 `git push`。

## 2026-08-29 具体算法命名、界面精简与十站位持续回归

### 实际代码改动

- 将精确信念、结构化发言、收益排序模拟策略、纯规则内核和完整分支树兼容适配器统一改为具体职责名称。代码标识符、docstring、运行日志和 GUI 不再使用算法代号、版本号或内部遍历方式作为用户可见名称；数据库兼容字段和归档文档文件名保持不变。
- GUI 标题与页签精简为“狼人杀策略展示”“对局分支”“发言收益”。发言收益页只保留站位、行动者和样本数三个必要输入，数据库路径、计算进程、批次、缓存、后验摘要与内部运行方式均不暴露给展示用户。
- GUI 的按钮、表头、状态、弹窗、hover、动作说明以及运行日志自然语言全部通过 `t(...)` 取得；中英文词条保持键集合一致。中文界面统一使用“宽区间 / 窄区间”“忽略发言 / 中等影响 / 较强影响”等面向展示的表达。
- 完整分支树启动日志不再输出内部遍历缩写；崩溃文件标题、Python 原生崩溃警告和底层异常日志也接入同一 `t(...)` 词条表，保持所选语言一致。
- 决策矩阵批次结束后显式关闭计算进程句柄、任务队列和结果队列，并等待队列 feeder 线程退出，避免连续计算多个站位时累积 Windows 多进程资源。
- 修正 GUI 协调进程完成日志的格式字段：终态事件已经成功生成时，不再因日志模板参数名不一致把完成结果误报为失败；增加直接覆盖协调进程终态事件的回归测试。
- 十站位测试使用固定随机种子抽取 10 个不同真实站位，并断言每个观察安全请求均完整输出三个可信度档位、全部动作行、样本计数和闭合的六类情景计数。测试输入与观察等价复用说明同步记录在 `DECISION_MATRIX_TEST_REPORT.md`。

### 设计难点与代码修正

- 10 个真实站位不必对应 10 个不同查询键：行动者无法区分的完整站位必须映射到相同的角色视角、精确 posterior 摘要和请求身份。回归保留 10 个真实输入，同时明确验证 6 个观察安全请求的合法复用，避免为追求摘要去重而把上帝视角信息写入键中。
- 首轮连续多站位回归暴露出子进程和队列句柄未释放的问题，单例测试无法发现该缺陷。修正后的相同 10 站位用例在 8.08 秒内完成，并成为常驻回归测试。
- 真实窗口首次完成发言收益计算后显示 22 种表达和 `3/3` 计算单元，但完成日志使用了不存在的格式字段，最终弹出失败提示。日志字段契约修正后，自动化终态测试和真实窗口复验均得到完成弹窗。
- 展示文本的目标受众具备基础理论知识但不了解算法目的，因此界面只解释“输入什么、比较什么、结果如何阅读”；内部调度、遍历、缓存和数据库术语保留在代码设计与更新文档，不进入 GUI。

### 实际验证

- 项目 `.venv` 下执行完整模块回归：`tests/test_search_simulator_tree.py` 与 `tests/test_search_simulator_matrix_gui.py` 共 56 项全部通过；其中包含日志调用只能使用国际化词条的静态守卫。
- 固定 `random.Random(20260829)` 抽取站位 `1236、845、699、144、86、116、296、57、70、130`；10/10 输入均得到完整矩阵，动作行数按角色为 22 或 33，三个可信度档位齐全，每个单元样本数与六类情景计数一致。
- Ruff 格式化后对模块和两份测试执行 `ruff check`，全部通过；导入顺序问题已自动修正。
- 使用 Computer Use 启动真实 Windows GUI，完成“对局分支”启动页、“发言收益”页面、计算完成弹窗、动作行选择、详情更新和 hover 验收。首次发现的完成日志异常修正后重新运行成功；最终中文词条复查确认“宽区间 / 窄区间”一致、无明显重叠或混合语言，关闭后无残留窗口。
