# AGENTS.md — search_simulator 模块约束

本文件仅约束 `search_simulator/` 子模块。该模块在设计、实现和文档上独立于父项目；处理本模块任务时，以本目录文件为准，父级项目文档不构成本模块算法设计的事实来源。

## 开工顺序

修改本模块前必须依次阅读：

1. `AGENTS.md`
2. `PROJECT_ALIGNMENT.md`
3. `ALGORITHM_DESIGN_V3.md`
4. `CODE_CHANGES.md`
5. `CODE_DESIGN_V3.md`（若需核对完整分支树迭代的历史实现映射，再读 `CODE_DESIGN.md`）
6. `STRATEGY_IMPLEMENTATION.md`

若用户提出新的设计要求，先更新对应文档并确认文档边界，再修改代码。

## 文档职责边界

- `PROJECT_ALIGNMENT.md`：记录当前研究问题、目标规则集、提示词信息边界、实验组、baseline 和待确认决策；待确认内容不是算法事实。
- `ALGORITHM_DESIGN_V1.md`：归档完整分支树迭代与固定 DAG 后处理方案；文件名中的编号只表示文档沿革，不是算法名称。
- `ALGORITHM_DESIGN_V2.md`：归档有限隐藏世界精确贝叶斯信念追踪方案；文件名中的编号只表示文档沿革，不是算法名称。
- `ALGORITHM_DESIGN_V3.md`：当前算法真源，只记录精确信念、结构化 cheap talk、角色视角、功利等级策略、模型条件 Monte Carlo、状态、信息约束、数学公式、复杂度、风险和设计不变量；不得记录实现文件、类名、函数名、数据库表、迁移步骤、依赖安装或测试命令。所有数学公式必须使用独占行的 `$$ ... $$` 公式块，不使用行内公式。
- `CODE_CHANGES.md`：记录实际实现映射、代码改动、数据结构、持久化方案、迁移方案、依赖和验证结果；本文件是代码更新事实的主文档。
- `CODE_DESIGN.md`：保留既有实现映射和历史上下文；新增代码改动先写入 `CODE_CHANGES.md`，不得把代码清单复制到算法文档。
- `CODE_DESIGN_V3.md`：记录精确信念 Cheap-talk 决策矩阵的文件职责、接口、并发拓扑、数据库表、恢复逻辑、输出契约和验证计划；它是设计方案，实际完成事实和验证结果必须同步写入 `CODE_CHANGES.md`。
- `STRATEGY_IMPLEMENTATION.md`：记录战术实现难点、边界条件、失败模式和工程注意事项。
- 数学公式保留在 Markdown 文档中，当前算法真源的全部公式统一使用独占行的 `$$ ... $$` 数学渲染语法，不使用行内公式或带 `latex` 语言标识的代码块包装。
- Markdown 文档中的流程、状态机、依赖拓扑和分支关系必须使用带 `mermaid` 语言标识的围栏代码块表达，不得再用 `text` 代码块、ASCII 框线或字符箭头伪装流程图；命令、复合键、阶段枚举和非图形化示例仍按其本来语义使用普通代码块。其中显示文本用中文表示。
- 本模块的文档修改只写入 `search_simulator/`，不得为了同步而修改父级文档。

## 算法命名与展示文案（强制）

- 文档文件名中的历史编号只用于定位文档沿革，不得作为算法名称写入代码标识符、注释、docstring、日志、异常、线程或进程名称、CLI 帮助、GUI 文案、结构化输出和新增测试名称。
- 完整未来分支枚举统一称为“完整分支树迭代”；固定图后处理按语义称为“状态 DAG 区间回传”。
- 当前决策算法统一称为“精确信念 Cheap-talk 决策矩阵”；其采样阶段称为“前向终局 Monte Carlo”，行为策略称为“功利等级策略”，角色信息层称为“角色视角精确条件化”。
- 规则、动作、角色视角、策略和随机数方案若需要进入缓存身份，必须使用描述职责的稳定规范标识，不得使用 `v1`、`v2`、`v3` 等数字版本代号。
- 既有 SQLite、pickle 或紧凑状态中的历史格式标记只允许留在兼容读取分支；它们是存储格式，不是算法名称，不得进入 GUI、普通日志、异常提示或新输出。
- GUI 面向具有部分理论基础但不了解本项目目的的展示观众：只解释“输入是什么、正在做什么、结果如何读”，隐藏 DFS/BFS、SQLite、posterior、rollout、common random numbers、进程拓扑和文档沿革编号；详细工程信息留在日志和文档。
- GUI 所有用户可见文本必须通过 `_i18n.py` 的语义键读取，并同时维护中文和英文资源；GUI 当前仍固定显示中文，不提供语言切换。

## 算法不可变约束

精确信念 Cheap-talk 决策矩阵的不变量以当前算法真源为准。以下涉及完整分支树迭代、DAG、GUI、SQLite 搜索结果和 wide/narrow interval 的约束继续适用于既有搜索器兼容路径，不得反向要求精确信念层展开完整未来树，也不得把完整分支树迭代的结构路径计数当成决策矩阵的行为概率。

### 精确信念 Cheap-talk 决策矩阵

- 本算法分为精确信念层和前向终局 Monte Carlo 决策层。信念层对有限合法站位精确条件化，不用 Monte Carlo 近似 posterior；决策层从完整 posterior 抽样隐藏世界，并用单独规范化的功利等级策略边缘评分投影模拟未发生未来到终局。
- 决策层与 LLM 完全解耦：不得调用 LLM、解析 LLM 响应、使用 LLM 采样未来行动或把 prompt/自然语言文本写入动作键。模块只输出结构化矩阵。
- 决策矩阵不依赖历史对局拟合行为概率。Monte Carlo 无偏性只指对显式规则、显式算法策略和显式随机机制定义的模拟分布无偏，不得表述为真实玩家或 LLM 行为无偏。
- 现有模拟器的规则逻辑应抽取为无策略、无行为概率的纯规则内核；规则强制的随机结算通过显式随机源注入。完整分支树迭代通过兼容适配器保留原功能，不得删除旧入口、DAG、SQLite solution 或 interval 功能。
- 规则内核只负责合法动作、单动作状态转移、阶段结算和终局判断。`smart_vote`、战术选择、优先级、softmax、Monte Carlo、缓存和 UI 都不属于规则内核。
- 决策矩阵内部必须使用逐席位发言、逐玩家投票和昼夜微阶段；不得用完整分支树迭代的整日 tactic profile 代替“某玩家发言前”的决策状态。
- 一级动作族固定为 baseline、指控、支持、投票意向、跳预言家和沉默，最多六类；二级具体动作展开目标、查验结果、强度和战术标签。一级只用于摘要，二级必须保留全部同级目标。
- 完整假跳预言家必须给出其他存活席位及好人/狼人结果；弱身份声明是独立动作。真预言家只能公开真实查验。预言家不发言固定为发言位置正常经过且内容为空的主动沉默事件。
- 发言证据强度范围固定为 `[0,1]`，当前三档为 `0 / 0.5 / 0.8`；零档表示忽略发言证据，不表示声明必假。softmax 温度固定为独立参数 `0.25`，不得与可信度混用。
- rollout policy 固定遵循“硬规则 > 战术硬约束 > 阵营目标等级 > 身份能力等级 > 同级动作”，五档规范分数为 `1 / 0.75 / 0.5 / 0.25 / 0`；完全同级动作必须同概率。
- rollout 中未来玩家不得再次构建收益矩阵、启动嵌套 Monte Carlo、MCTS 或调用 LLM。
- 每个二级具体动作、每个可信度档位固定执行 100 条终局轨迹；不得根据中途领先结果早停后仍把普通样本均值称为无偏估计。修改样本数必须改变规范请求身份。
- 相同可信度和样本索引下，所有候选复用隐藏站位、后续行动、平票和规则随机流。候选顺序、进程数量和任务完成顺序不得改变逐值结果。
- 六类反应情景按“行动者反噬 > 目标成功转移 > 声明被接受 > 声明受到争议 > 发言被忽略 > 其他”互斥分类，默认信念变化阈值为 `0.05`。情景只用于解释，不参与收益或策略。
- 角色视角查询键必须显式包含 `actor_id`、`actor_role`、依法知道的站位硬知识和 posterior 摘要。狼人只获得依法知道的狼队信息，预言家查验好人只形成阵营约束；公开声明、死亡和高 posterior 不得升级为硬知识。
- 其他玩家真实角色和上帝视角完整站位不得进入行动者矩阵查询键或输出。第一版不跨行动者执行座位重标后的缓存复用。
- 矩阵请求先由对局配置、观察者安全决策状态、行动者角色视角、posterior、候选集合、policy、可信度、样本数和种子方案定位，再由具体 action 与可信度定位单元格。
- 矩阵只输出具体动作的收益均值、Monte Carlo 标准误、相对 baseline 配对差、样本数、三档敏感性和情景计数；不得把三个可信度档位合并成未经定义的单一分数。
- 前向终局 Monte Carlo 不构造完整未来 DAG，不保存全部轨迹，不使用完整分支树迭代的 multiplicity、LRU、path count 或 wide/narrow 作为概率。
- 决策矩阵计算默认使用两个隔离计算子进程；CPU 模拟不在线程执行。父进程线程只负责聚合和 SQLite 单写，任务、结果和持久化队列必须有界并形成背压。
- 任务按“可信度档位 + 样本索引批次”切分，一个 worker 任务计算全部候选以保留 common random numbers。worker 只返回 count/sum/sum-of-squares、paired delta 和情景计数，不跨进程传输完整轨迹或 DAG。
- GC 只在批次边界管理，不在逐轨迹热循环强制全代回收；worker 必须定期回收，父进程必须执行物理内存安全检查。进入安全区时停止派发并写为可恢复中断。
- 批次必须以稳定 `batch_id` 和样本索引区间幂等提交；worker 重试使用原索引和原种子，重复结果不得二次累加。失败样本不得换种子补抽。

### 完整分支树迭代与界面

- 精确信念层不展开完整未来对局树；决策层只执行固定功利等级策略下的单轨迹前向终局 Monte Carlo，不构造完整未来 DAG。既有搜索器继续使用完整分支树迭代，仅作为历史方案和小规模规则验证路径。
- GUI 固定使用 DFS，不显示运行模式；BFS 只能通过 CLI 显式启动。
- GUI 使用 Pygame / pygame_gui，默认中文，不提供语言切换按钮；底层 i18n 与 CLI 英文输出能力保留。
- 任何 GUI 代码、布局、样式、交互、hover、动画或可视化改动，除静态检查和自动化测试外，必须使用 Computer Use（电脑工具）启动真实 Windows GUI 做视觉验收；验收至少覆盖启动画面、改动所在页面及其关键交互状态，并把实际观察结果记录到 `CODE_CHANGES.md`。Pygame dummy smoke、离屏截图或坐标回归不能替代真实窗口视觉验收。
- GUI 标签不得重叠；原生控件与自绘战术项、表格行、DAG 节点和边都必须提供可见 hover 反馈，边 hover 可查看完整派生原因。
- GUI 参数输入框必须提供 hover 文本说明参数含义与合法范围。
- GUI 的 `lambda` 参数固定使用滑块与实时文本显示组合，范围 `[0,1]`，默认 `0.5`。
- GUI 所有角色、运行模式和战术勾选项必须提供 hover 文本；不可选战术仍需显示禁用原因。
- GUI 迭代时按已完成站位实时更新终局节点、好人路径、狼人路径和当前路径占优关系；不得把路径占比表述为概率。
- GUI 点击开始后必须立即进入可见运行状态，并在单个站位尚未完成时持续显示已展开节点、已发现节点、frontier、边数和当前站位；这些计数不得伪装为完成百分比。
- 站位进入 worker 后必须立即以“计算中”状态显示；DAG 面板必须在运行中接收增量节点和边，连续展示从站位分布到树展开的局部实时预览，不得等站位完成后才首次绘制。
- GUI 进度和 DAG 数据固定每 0.5 秒批量刷新；Pygame 交互循环必须保持正常帧率，不得将 hover、点击或暂停/恢复降为 0.5 秒轮询。
- 搜索后的路径汇总、节点 interval 逆拓扑回传和边 interval 同步必须分别向 GUI 发布阶段事件，至少每 0.5 秒显示已处理量/总量；用户调整 `lambda` 后的动态 interval 重算也必须在后台执行并显示进度，不得阻塞 Pygame 绘制循环或让界面停在最后一个搜索节点上。该比例只表示固定 DAG 后处理进度，不得表述为搜索完成概率。
- 新进入可见子树的节点必须淡入，已存在节点更新不重复闪烁。DAG 初始只显示局部入口；点击节点展开其子节点，再点父节点收起其子树，布局和绘制只处理当前可见子图。
- DAG 必须使用固定网格布局：X 轴按迭代深度逐列递进并保留深度空档，同一深度按稳定节点顺序从上到下占用固定网格行；视口绘制带数字坐标的横轴，并用竖向虚线分隔并标注每个深度区间。网格坐标、分隔线和轴刻度只属于 UI 观测，不得进入状态签名、DAG 合并或 interval。
- DAG 必须提供当前有界观测图的“全部展开 / 全部收起”控件；该控件只能改变本地可见子图，不得触发搜索或修改 SQLite 图。
- 实时预览必须有界并明确属于观测窗口；为保持 UI 响应而丢弃旧预览节点不得影响完整搜索、SQLite DAG 或最终结果。
- GUI 必须提供暂停/恢复控件；暂停应保留 frontier、DAG 和已计算计数，并阻止 worker 展开下一节点，恢复后从原位置继续。
- DAG 节点保持紧凑显示，hover 时必须完整展示该节点 GameState 的所有字段及每名玩家的全部字段，不得仅显示摘要或固定前几行。Hover 必须按正常 UI 文本使用中文标签、分区标题和逐行玩家卡片式信息，不得直接渲染 JSON、Python 字典或带引号的机器字段。完整快照只用于 UI/持久化观测，不得改变状态签名与 interval。
- 搜索节点不得为长期内存保留重复的完整嵌套 GameState 字典；必须保存无损紧凑快照，只在实时批次、API 输出或 hover 时按需重建完整 GameState。
- interval 回传必须使用单遍 count/sum/min/max 聚合，禁止为同一父节点构造与子节点数量成比例的 `RewardInterval`/`RobustIntervals` 临时列表。
- interval 加速必须以基准和峰值内存为依据。当前字典 DAG 与变长邻接表不得仅为使用 NumPy 而复制为百万级致密数组；只有在确认端到端耗时明显下降、结果逐值等价且峰值内存不劣化时才允许引入 NumPy 路径，否则保持标准库单遍流式实现。
- GUI 运行搜索时必须始终使用隔离子进程，即使 `parallel_workers=1` 也不得在 Pygame 主进程的普通后台线程中直接展开搜索；计算 worker 的导入图禁止包含 Pygame、SQLAlchemy 或 greenlet，发现污染时必须在展开前转为 GUI 可见错误；子进程异常不得直接破坏 Pygame 主循环。
- 跨进程进度与完整结果传输必须使用有界队列和背压，不得把完整站位 DAG 作为无限积压的任务结果留在父进程；每个 worker 同时最多保留一个未完成站位任务，节点和边由单一 SQLite 写入者分批消费。
- 全站位搜索固定采用“单站位完整迭代 → 完整持久化并提交检查点 → 释放该 worker → 下一个站位”的串行调度；不得并发展开多个站位，也不得在当前站位完成持久化前预取下一站位。
- 每个已完整持久化的站位都是可恢复检查点。相同规范配置再次启动时必须自动复用最近的未完成运行、校验节点/边数量完整性并跳过已完成站位；不得只依赖内存计数判断完成。
- 系统可用内存进入安全保留区时必须自动中断当前批次并把运行标记为可恢复中断。已完成站位不得回滚；未完整写入的站位不得作为检查点，下次只允许重算该站位。
- 每次运行必须且只能落入 `complete`、`interrupted` 或 `failed` 三种终态。logger 必须输出终态、`run_id`、已完成/总站位、下一恢复站位和异常堆栈；worker 进程异常退出或原生崩溃必须由父进程记录为 CRITICAL，不得表现为无日志的自动停止。任何被协调器或 GUI 捕获并归类为 `failed` 的 Python 异常也必须同步追加到本次时间戳 crash 日志，正文包含 PID、运行上下文和完整 traceback；`failed` 终态不得留下空 crash 文件。
- CLI 与 GUI 都必须把运行日志写入 UTF-8 日志文件；程序启动时必须在模块根目录创建 `crash_log/`，并为本次启动生成只含一个高精度运行时间戳的独立 UTF-8 crash 日志，文件名不得加入 PID。父进程和本次派生 worker 共用该文件，下一次启动不得覆盖或混入旧 crash 日志。日志正文必须记录实际 PID/线程信息用于定位，但不得用 PID 拆分同一次运行的日志文件。运行日志与 crash 日志路径必须在启动日志、终态日志和 GUI 弹窗中可见。
- GUI 必须为三种终态分别显示模态弹窗：正常完成、可恢复中断、运行失败。弹窗必须明确说明是否已经完成、检查点进度、下一恢复站位和日志路径；失败弹窗不得复用“完成”文案。
- 若 access violation 直接终止承载 GUI 的进程，当前进程无法继续绘制弹窗；下一次启动必须检测最近一个非空且尚未提示的时间戳 crash 日志，弹窗明确告知“上次运行原生崩溃”，显示日志路径，并在确认提示后记录已通知标记。
- GUI 是研究者上帝视角；算法决策必须严格使用对应玩家可见的信息。

### SQLite 访问

- 模块内所有 SQLite 建表、索引、查询、upsert、更新、删除、统计和 schema 检查必须使用 SQLAlchemy Core/Inspector API 构造；源码和测试中禁止明文 SQL、DDL、PRAGMA 及其字符串拼接。
- 持久化 API 必须封装连接与事务，不得向 GUI、搜索层或测试暴露 DBAPI cursor 作为常规读写通道。
- 运行创建、检查点更新、可恢复运行查找、完整站位校验和中断状态写入都必须经过持久化 API；异常退出或人工中断后，不得要求用户手工修改数据库才能续算。
- 数据库必须保留 `lru`、`memory`、`solution` 三类职责明确的表：`lru` 只存状态签名热缓存，`memory` 只存运行/断点检查点，`solution` 只存完整且通过节点/边计数校验的可复用解索引；三者不得用同一列语义相互替代。
- 每次运行请求先查询 `solution`。命中时首次请求只加载已有摘要与可按需读取的 DAG，不展开新节点；用户再次点击运行或 CLI 显式传入强制重算参数后，才创建新 `memory` 批次继续 DFS/BFS。`running`、`interrupted`、`failed` 记录不得伪装成 `solution`。
- solution 配置键只包含影响未来转移的规范搜索配置；interval 的 `lambda` 属于完成后的观测参数，不得阻止同一 DAG 的解复用。
- 决策矩阵与完整分支树迭代结果使用同一个 SQLite 文件，但必须新增职责独立的矩阵运行、矩阵行和幂等批次表；不得复用 `lru`、`memory`、`solution` 或图表表达决策矩阵。
- 矩阵运行表保存完整规范请求身份、行动者角色视角、posterior 摘要、规范标识、样本目标和 `running/complete/interrupted/failed` 状态；矩阵行按 `matrix_id + canonical_action_key + credibility` 唯一定位；批次按 `matrix_id + credibility + batch_id` 幂等提交。
- 只有在三个可信度档位、全部二级动作、每行目标样本数、情景计数、baseline 配对样本和批次区间全部校验通过后才能标记 `complete`。`interrupted` 允许按原样本索引恢复，`failed` 必须保留失败证据并显式新建运行。
- 决策矩阵计算 worker 禁止导入 SQLAlchemy；父进程单一写入线程在事务内登记批次、累加充分统计量和推进运行状态。重复 batch 消息不得二次累加。

### 智能投票与战术

- 默认开启智能投票。
- 仅在智能投票模式中显示、选择和执行战术；关闭智能投票时，战术集合必须为空。
- 决策优先级固定为：游戏硬规则 > 战术硬约束 > 阵营理性筛选 > 身份启发式 > 同级目标全部展开。
- 投票先考虑阵营目标，信息不足时才退化为个人身份启发式。
- 同级目标不得随机抽样或按座位号裁决，必须分别扩展分支。
- 智能模式是功利理性的启发式剪枝，不得宣称为已经证明的全局最优、极小极大均衡或完美贝叶斯均衡。

### 信息与身份

- 除愚者发动“身份揭示”外，所有死者身份不公开；死亡本身不得新增公开身份知识。
- 狼人知道狼队成员；预言家仅自己知道查验结果；其他好人只能使用公开信息。
- 上帝视角可展示真实身份、公开宣称、公开确认和预言家私有查验，但这些信息层级不得在算法中混用。
- 愚者属于好人阵营神职，默认板子不启用。其白天被投票出局时免死并公开身份，当天立即结束；此后永久失去投票权，也不再成为智能投票目标。

### 状态、DAG 与缓存

- 仅合并未来转移完全等价的状态。
- 父子边必须保留所有派生原因；相同父子节点的不同动作、目标或战术原因不得只压缩为一个代表动作。
- LRU 签名只包含规范化的游戏迭代状态；reward、interval、界面状态、边和派生原因都不得进入签名。
- 状态签名编码必须采用非递归、无闭包热路径；签名实现须通过至少一百万次同状态连续计算压力回归，且结果始终确定、进程不发生 access violation 或对象类型错位。
- 多路径计数和 multiplicity 可以独立保留，但不得影响 interval 向后传播。

## Reward interval 冻结规范

除非用户明确要求，不得擅自修改以下定义。任何与旧实现、旧文档或论文的冲突均以本节为准。

- 删除 Wilson 置信区间和 `confidence_level`。
- interval 是搜索完成后的观测量，不参与分支生成、智能投票、战术选择、目标排序、剪枝、状态签名、DAG 合并或 LRU。
- 搜索完成后允许用户动态调整 `lambda`，只对选中 DAG 重新回传 wide/narrow 并更新颜色，不得重新迭代；其他站位按选择惰性重算。
- 只使用 `wide_interval` 与 `narrow_interval`；不使用“乐观/保守”解释，不宣称统计置信度。
- 默认风险参数 `lambda = 0.5`，且 `lambda` 取值范围为 `[0, 1]`。
- 好人终局为 `[1, 1]`，狼人终局为 `[-1, -1]`，未决或没有可计算子节点的状态为 `[-1, 1]`。
- 每个节点只考虑其唯一的直接子节点集合；忽略父节点、多边合并关系、边 multiplicity 和多条派生原因。
- wide 使用子节点下界最小值、上界最大值与对应均值加权；narrow 使用子节点下界最大值、上界最小值与对应均值加权。
- narrow 始终执行既定的反转设计；计算后若下界大于上界，则交换边界，确保显示时上界不小于下界。不提供反转开关。
- 分支颜色永远依据子节点的 `wide_interval`：先判定符号，同为非负且不全为零时为蓝色，同为非正且不全为零时为红色；只有跨零时才比较上下界绝对值，差小于 `0.001` 为黑色，上界绝对值更大为蓝色，否则为红色；`[0,0]` 为黑色。

具体公式只以 `ALGORITHM_DESIGN_V3.md` 为准。若用户明确改变 interval 设计，必须先修改算法文档和本文件，再改代码。

## 当前研究板子与战术

- 当前研究板子固定为 7 人：2 狼人、2 平民、1 女巫、1 预言家、1 守卫，共枚举 1260 种站位；当前实验不启用愚者或其他角色。
- 当前阶段只研究第一天白天 cheap talk 对公开票型和阵营终局收益的影响，玩家人格固定为理性、功利、同质。
- 精确信念 Cheap-talk 决策矩阵在每名存活玩家轮到第一天发言前输出行动者视角的结构化收益；矩阵只输出，不处理 LLM 响应，也不依赖历史对局或 LLM 采样。
- 白天主研究战术：平民跳预言家挡刀、预言家不发言、狼人悍跳预言家；平民与狼人跳预言家分别区分前置位前四和后置位后三。
- 完整平民挡刀和狼人悍跳必须包含查验目标与好人/狼人结果；弱身份声明单独建模。预言家不发言固定为空内容的主动沉默事件。
- 平票固定为最高票并列者中均匀随机放逐；女巫、守卫、同守同救和同时终局优先级以 `PROJECT_ALIGNMENT.md` 的已冻结规则为准。
- 黑夜扩展战术：狼人自刀骗解药、狼人空刀；它们不属于当前三个白天主处理变量，参与同一实验时必须单独列为控制变量。
- baseline 不启用平民挡刀和狼人悍跳，预言家必须有效发言；未启用战术不得先注入 system prompt 再试图仅用低优先级 chat prompt 覆盖。
- 每个实验单元至少运行 10 局独立重复；该经验结果不得自动解释为无偏总体期望。
- 黑夜战术仅在女巫或守卫存在时允许选择；狼人自刀还要求运行时存活狼人数量至少为 2，并枚举每名存活狼作为自刀目标。
- 目标规则、提示词信息契约、baseline 和待确认决策以 `PROJECT_ALIGNMENT.md` 为准；其中标为待确认的语义不得由实现层采用常见房规自行补全。

## Python 与运行环境

本模块与父项目共用 Python 3.12 虚拟环境：

```powershell
cd D:\Codes\AIwerewolf\search_simulator
..\.venv\Scripts\Activate.ps1
python -m search_simulator --cli
```

GUI：

```powershell
python -m search_simulator
```

BFS 只能从 CLI 显式启用：

```powershell
python -m search_simulator --cli --search_mode bfs
```

若 PowerShell 执行策略阻止脚本，使用模块内的 `run_cli.cmd`。中文终端建议启用 UTF-8：

```powershell
chcp 65001
$env:PYTHONUTF8 = "1"
$env:PYTHONIOENCODING = "utf-8"
```

## 修改与验证纪律

- 使用模块自己的设计文档指导实现，不从父项目同名概念推断本模块行为。
- 保留用户已有改动，不重置或覆盖无关文件。
- 每次单元测试、CLI smoke、压力测试或 GUI 验证结束后，无论成功、失败或中断，都必须立即删除本次测试生成的临时文件和临时目录；测试优先使用 pytest `tmp_path` 或模块 `tmp/` 作为隔离位置。清理范围包括临时 SQLite、JSON、图片、日志、下载包、解压目录和专用字节码缓存，但不得删除正式运行检查点、`crash_log/` 历史崩溃证据、用户指定输出或无法确认归属的文件。确需保留测试证据时必须明确说明文件及理由。
- 新增或修改的关键函数、类、复杂状态转换、并发/进程边界、持久化事务和恢复逻辑必须添加准确的中文 docstring 或中文行内文档注释；注释应说明设计意图、不变量和失败处理，不得用逐行翻译代码的噪声注释充数。
- CLI/API → 模拟器、父进程 → worker、搜索层 → 持久化层的参数必须通过稳定的具名字段显式传递；新增边界参数必须在中文 docstring 中说明含义、范围和返回值，不得依赖不透明位置参数、隐含进程全局状态或未记录的默认值。
- 修改代码后至少验证：静态编译、模块测试、两种完整分支树遍历 CLI、SQLite 持久化恢复，以及 Pygame GUI 启动。实现精确信念 Cheap-talk 决策矩阵时还必须验证规则内核差分、角色视角隔离、小型精确期望与 Monte Carlo 对照、共同随机数、`1/2/4` worker 确定性、批次幂等、中断恢复和峰值内存。
- `CODE_DESIGN_V3.md` 记录精确信念 Cheap-talk 决策矩阵的设计约束和实现计划；实际执行结果写入 `CODE_CHANGES.md`，不得把计划内容冒充已验证事实。
