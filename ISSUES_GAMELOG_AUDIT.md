# 原版本对局日志审计与修复记录（2026-09-05）

> 审计方式：基于 main@32a3695，运行 2 局基线对局（seeds 9201/9202，DeepSeek deepseek-v4-flash，
> rule_preset=baseline），对照廉价磋商研究的问题清单（拼接占位、幻觉、投票理由-目标一致性、
> 状态记忆、规则遵守）逐项核查，并下钻到代码定位根因。
> 每条 issue 标注：严重度（P0 影响对局正确性 / P1 影响数据质量 / P2 文本瑕疵）、证据、修复状态。

---

## P0-1 structured 决策管线不注入 memory → 玩家对自己的行动"失忆"

**证据（9202）**：女巫卡卡夜 1 用解药救了 3 号穆冬青（transcript 第 68 行，行动记录完整），
但白天发言内心理由写"作为女巫我昨晚未用药（因无人死亡）"，投票理由写"我清楚自己昨夜
没有被刀也没有使用解药（因为无人可救也没中刀）"——把自己的救人误读成"没人被刀"。

**根因（代码确认）**：`structured_decision.build_structured_user_prompt` 只拼
`game_context + observation + strategy_bias + catalog`，**没有 `memory.format_for_prompt()`**。
`role_state["save_used"]` 在 `agent.witch_act` 里被正确设置，但新管线的 prompt 根本不包含它。
Memory 模块（判断、角色状态、行动记录）在 structured 路径下整体失效。

**修复**：`build_structured_user_prompt` / `build_repair_prompt` / `run_structured_decision` /
`Pipeline.run_structured` 增加 memory 参数并渲染 `memory.format_for_prompt()`；
agent.py 全部 `run_structured` 调用点传入 `memory=self.memory`。

## P0-2 决策 max_tokens=384 → 理由被截断

**证据**：9202 卡卡投票理由断在"暂时"、凌战投票理由断在"的中"；9201 女巫毒药理由断在
"与袁汐对跳的"。截断的投票理由直接参与放逐了真预言家（9202 Day1）。

**根因**：`pipeline.run_structured` 中 `max_tokens = 1536 if require_speech else 384`。
structured 格式的理由动辄 200+ 字，384 token 不够。

**修复**：决策分支提到 1024（对齐已验证实现；speech 保持 1536）。

**修订（2026-09-05 验收轮）**：1024/1536 之后仍见截断（9303 女巫毒药理由断在"若莫离是
好人则"）。逐层排查后确认**真正的根因不是 max_tokens，而是 `agent._decision()` 里对
reasoning 的硬切片 `reasoning[:200]`**——9303 被截理由实测恰好 200 字符，9312 狼队空刀
理由同样断在 200 处。修复：`reasoning[:200]` → `reasoning[:1000]`。
同时**将全部决策 max_tokens 上限统一为 4096**（该上限对模型不可见，仅作安全阀，
防止未来再出现预算侧截断）：`pipeline.run_structured` 4096、`run_vote_line` 4096、
`direct_call`/`_call_legacy` 默认 4096、全部 `_legacy_*` 分支 4096、`structured_decision`
两处默认 768→4096、`agent.py` 两处显式 300 上限改为走默认、`factory.py` 客户端兜底默认
768→4096、`reflect.py` 反思 800→4096。已验证版本实现同步修改（agent_loop speech 768/
decision 1024→4096、同类 legacy 上限、`reasoning[:200]`→`[:1000]`）。

## P0-3 投票理由与目标错位仍会发生

**证据（9202 Day1）**：凌战投票理由："投目前最有起跳争议风险的4号**不太合理**……
**给1号一票**……"，记录目标却是 **4号赵铁柱（真预言家）**。该错位票是 Day1 放逐真预言家的
三票之一，直接导致狼胜。

**根因**：structured JSON 里 `reasoning` 与 `target_seat` 是两个独立字段，无一致性约束；
理由截断时更容易与目标分叉。

**修复**：投票改用已验证的严格单行文本格式——模型必须输出"我投给X号，理由是……"，
目标只从该句的 X 解析（行首锚定优先、全文最后一次声明兜底、全角数字归一化、失败重试一次），
理由文本中提及的其他玩家不再参与目标解析。

## P0-4 规则文案自相矛盾 → 平民把真预言家起跳当可疑点

**证据（9202 Day1）**：穆冬青（平民）投票理由："4号跳预言家……**跳预言家本身不符合基线
打法**……选择投4号"——真预言家被 3 票放逐出局。

**根因**：B 组 rule_addendum 写"禁止使用任何特殊战术：**不得跳预言家**、不得悍跳……"（一刀切），
而引擎【规则公告】说"只有真正的预言家可以声称"。两处文案相互矛盾，玩家采信哪一处不可控。

**修复**：addendum 改为"**非预言家**不得跳预言家、不得悍跳、不得挡刀（即不得冒充预言家身份）；
狼队不得空刀、不得自刀"，并补"真预言家应如实公布身份与查验结果"，与【规则公告】对齐。

## P1-5 战术组 rule_addendum 泄露实验条件

**证据**：WJ/VJ/SQ 组 addendum 均为"本局为战术实验局（狼人悍跳条件）：其他玩家按标准方式
打牌"——非战术玩家被告知本局启用了什么战术（此前对局里守卫会在公开发言引用该信息），
破坏单变量对照。

**修复**：三组统一改为中性文案"本局为普通对局，所有玩家按标准方式打牌"，战术指令仅通过
`AIWEREWOLF_TACTIC_<ROLE>` 注入战术执行者。

## P1-6 BeliefTracker 声称/矛盾仍注入全部玩家 prompt 并驱动决策

**风险**：`_detect_role_claim` 虽已加固为第一人称匹配，但"如果我是预言家我会……"这类虚拟
语气仍会误报；`=== 角色声称 ===`/`=== 矛盾 ===` 注入所有玩家的 observation，矛盾记录还会
驱动 SocialModel 欺骗信号与投票重考虑逻辑。历史对局（seed 9004）中该机制曾造成"多人对跳"
混乱。声称信息本质上是公开发言的重复，注入属副作用源。

**修复**（行为等价于整体下线，保留提取层不动）：`BeliefTracker.format_for_prompt` 只保留
历史票型；`format_observation` 不再渲染声称块；`prompts.py` 移除 role_claims 注入；
`agent.py` 移除 contradictions→SocialModel 同步、role_claims 投票重考虑分支、矛盾反思条目；
`_find_sheriff_from_obs` 恒返回"无"（原循环遍历的 claim.context 从不含警长，属死逻辑）；
`wolf_team.build_wolf_team_view` 不再提取公开声称摘要。

## P1-7 狼人夜行动缺"第一夜无白天信息"约束

**证据（9202 Night1）**：狼人私聊与刀口理由出现"他**逻辑清晰**，留到后期威胁大"——
第一夜不存在任何白天发言，"逻辑清晰"无事实来源，属轻量幻觉。

**修复**：`attack` 与 `wolf_chat` 在 day≤1 时向 extra 注入"现在是第一夜，此前没有任何白天
发言。不要在理由中引用任何玩家的发言表现；可按座位位置或威胁假设选择刀口"（另一版本已验证）。

## P2-8 skip 夜行动渲染为 "→ ?"

**证据（9201 第 137 行）**：`[夜行动] 3号:林思远 skip → ?`——占位符污染，应显示"（无行动）"。

**修复**：`transcript.py` NIGHT_ACTION 渲染：无 target 的 skip 显示"（无行动）"，
其余空目标显示"（无目标/空刀）"。

---

## 审计中确认原版本实现正确的点（无需改动）

- 引擎侧能力门齐全：守卫禁自守/禁连守、女巫仅首夜可自救（actions.py）、毒药禁自用、
  狼禁刀队友、空刀/自刀按 preset 开关。
- `rule_preset` 抽象（baseline=诚实规则+禁空刀自刀 / cheap_talk=关闭）设计干净，
  战术组正确使用 cheap_talk 预设，诚实驳回不会误杀战术组。
- 【规则公告】+ 违规发言当场驳回的诚实规则强制：9201 Day1 全场正确采信真预言家、6:1
  放逐真狼，机制有效。
- 狼队共识轮（私聊后强制收敛同一刀口）两局运转正常。
- `_decision_from_data` 的目标解析收窄到 target 字段本身。
- 两局 0 引擎错误，胜负判定全部正确。

## 附带发现：修复前就存在的测试失败（与本次修改无关）

以下 2 个测试在未修改的 main@32a3695 上即失败，属随机投票装置
（game.py:600 公共随机装置）相关既有问题，建议后续单独排查：

- `tests/test_institution_switches.py::test_random_vote_device_designates_target_and_bypasses_agent_votes`
  （装置应全员归票同一目标，实测 P2 投给了 P1）
- `tests/test_institution_switches.py::test_random_vote_device_covers_all_alive_players_over_many_days`

## 修复后测试状态

游戏相关测试全部通过（test_cognitive_offline / test_engine / test_api /
test_institution_switches / test_action_catalog 等，153 passed；仅上述 2 个
既有失败保留）。search-simulator 的 2 个 GUI 测试因本机缺少 GUI 依赖无法
收集，与本仓库游戏逻辑无关。

## 回归验证（修复后，同 seeds 重跑两轮）

**第一轮（P0/P1/P2 修复落地后）**：
- ✅ P0-1：女巫两局均正确记得自己的用药状态（"我被刀了但解药已使用"/"我没有解药可自救"），
  修复前是"我昨晚未用药"的失忆陈述。
- ✅ P0-2：未再出现"暂时""的中"式截断。
- ✅ P0-3：全部投票理由与目标一致（逐票核对）。
- ✅ P1-7：夜 1 狼聊全部为座位位置论证，"逻辑清晰"式无来源引用消失。
- ✅ P2-8：skip 渲染为"（无行动）"。
- ✅ 奶穿（同守同救）等规则交互判定正确。

**第二轮（B 组 addendum 补充"未被驳回的声称即真预言家"说明后）**：
- 9201 好人胜：真预言家查验链被跟随（Day2 归票放逐其查杀对象），女巫夜 3 毒死最后一只狼。
- 9202 狼胜：预言家 Day1 存活（较修复前改善），但守卫自称身份被误读为"无法验证"而放逐，
  次日预言家被刀后平民归零。
- **残余（模型层）**：villager 对"身份声称"的信任度仍偏低，会以"未经验证"为由放逐声称者。
  引擎侧已无污染源，属 deepseek-v4-flash 判断质量问题，建议后续在 strategy 层补充
  "诚实规则下声称的可信度推理"指引并扩大样本观察。

---

## 四组完整验收审计（2026-09-05，B/WJ/VJ/SQ 各 1 局）

> 目的：确认修复后的对局过程可以"对标真实情景"——日志中的每票、每次夜行动、
> 每条记忆陈述都能与引擎状态对上，不存在引擎侧污染（说投 1 结果投 2、无中生有的
> 理由、失忆、渲染占位符等）。共跑两轮：第一轮 seeds 9301-9304（发现残留问题），
> 修复后第二轮 seeds 9311-9314、最终轮 seeds 9321-9324（见文末）。

### 审计方法

1. **投票理由-指向一致性**：正则抽取全部 `[投票]` 行，机械核对箭头目标与理由文本；
   多份投票理由以"目标名字，理由是……"开头（严格行格式下模型把名字接在座位号后），
   而箭头侧的"座位号:名字"由引擎按座位查名册生成——两者一致即证明模型声明行里的
   座位号与引擎落票座位是同一个。
2. **引擎规则核对**：夜行动→死亡公告→胜负判定逐局对账（奶穿/守卫挡刀/毒杀/空刀/
   parity 判定/平票随机放逐/女巫首夜自救限制/守卫连守限制）。
3. **记忆核对**：女巫用药状态、守卫上夜守护对象、全员对前一天票型的复述。
4. **截断检测**：扫描全部理由行是否以句中成分结尾。
5. **泄漏检测**：公开文本中的 addendum 文案、内部玩家 ID（P2-xxxx 格式）。

### 第一轮结果（seeds 9301-9304，修复后首次全量验收）

| 局 | 组 | 胜者 | 投票一致性 | 规则 | 记忆 | 截断 | 泄漏 |
|---|---|---|---|---|---|---|---|
| 9301 | B | wolf | 6/6 ✅ | 奶穿/parity ✅ | 女巫记得解药已用 ✅ | 无 ✅ | 无 ✅ |
| 9302 | WJ | wolf | 6/6 ✅ | 奶穿(女巫自救+同守)/3-3平票 ✅ | 守卫记得连守限制 ✅ | 无 ✅ | 无 ✅ |
| 9303 | VJ | village | 12/12 ✅ | 守卫挡刀/毒中真狼/ plurality ✅ | 女巫记得解药已用 ✅ | **女巫毒药理由断在 200 字** ❌ | 无 ✅ |
| 9304 | SQ | wolf | 12/12 ✅ | 奶穿/parity ✅ | 守卫连守+女巫解药 ✅ | 无 ✅ | 无 ✅ |

- 战术执行全部正确：B 组 d1claims=0（诚实规则生效）；WJ 狼双悍跳；VJ 平民假跳；
  SQ 预言家全程隐藏（内心理由明确写"按战术指令不暴露身份"）。
- **发现 2 个引擎级残留**（见下），其余全部通过。

### 新发现问题与修复

## P0-9 决策理由被硬切片 200 字符（P0-2 的真正根因）

**证据**：9303 女巫毒药理由恰好断在 200 字符（"……若莫离是好人则"）；9312 狼队空刀
理由同样断在 200 处（"……（连续"）。max_tokens 提高后仍复现 → 排查发现
`agent._decision()` 里 `reasoning=reasoning[:200]`，所有决策（夜行动/狼刀/发言）的
reasoning 入库前都被切到 200 字符。

**影响**：不影响行动目标解析（9303 毒药仍正确命中真狼），但日志失去可审计性——
理由缺失结论，无法据此复核决策。

**修复**：`reasoning[:200]` → `reasoning[:1000]`（已验证版本实现同步修改）。

## P1-10 投票跳过优化产出引擎模板理由 + 内部玩家 ID 泄漏

**证据（9313 Day2，7 号韩书研投票）**：理由为"发言立场未变: 投P2-9db6c8212da3（2号）"
——不是模型输出，是 `agent.vote()` 的 skip-optimisation：发言阶段已产生投票意向且
"无新信息"时跳过 LLM 投票调用，直接复用意向并拼装模板理由；模板里把内部玩家 ID
（P2-9db6c8212da3）拼进了公开投票记录。

**影响**：目标本身与意向一致（无错位票），但投票理由不是玩家的话，且 ID 泄漏进
公开事件，破坏日志的真实感与可读性。

**修复**：删除该优化（已验证版本实现同步修改），每一票都走严格单行文本产生模型自己的理由。
保留"唯一合法目标"优化（仅剩 2 人时，理由语义完整且使用座位号:名字）。
修复后测试 115 passed（test_cognitive_offline + test_engine）。

## P2-11 狼队空刀在"当前狼队票/狼队计票"明细里渲染为 "→?"

**证据（9312 Night2）**：`[夜行动] 4号:雷昊 attack → （无目标/空刀）…｜当前狼队票:
4号:雷昊→?`。P2-8 只修了行动目标渲染；狼票明细走 `_public_label`，其对空 target
返回 "?"（truthy），导致调用方的 `or '空刀'` 兜底永远不触发。

**修复**：`_public_label` 空值返回空串，让 "空刀" 兜底生效（已验证版本实现同步修改）。

### 第二轮结果（seeds 9311-9314，4096 + [:1000] 生效）

| 局 | 组 | 胜者 | 投票一致性 | 规则 | 记忆 | 截断 | 泄漏 |
|---|---|---|---|---|---|---|---|
| 9311 | B | wolf | 11/11 ✅ | 奶穿/守卫挡刀/parity ✅ | 守卫记得连守 ✅ | 无 ✅ | 无 ✅ |
| 9312 | WJ | village | 13/13 ✅ | 空刀合法/女巫自救/ plurality ✅ | 女巫+守卫 ✅ | **狼刀理由断在 200 字**（P0-9 当时未修）❌ | 无 ✅ |
| 9313 | VJ | wolf | 12/12 ✅ | 毒/刀/parity ✅ | 女巫+守卫 ✅ | 无 ✅ | **1 条模板理由+ID 泄漏**（P1-10）❌ |
| 9314 | SQ | wolf | 6/6 ✅ | 奶穿/parity ✅ | 守卫复述 D1 全部 6 票逐票准确 ✅ | 无 ✅ | 无 ✅ |

### 模型层残余（引擎侧已无污染源，非本次修复范围）

- 9311 守卫白天内心理由写"我昨晚守了5号（P2-…）"——实际守的是 2 号（行动记录与
  挡刀结果均正确），模型在散文里写错座位并回显了 prompt 中的玩家 ID。
- 9313 女巫内心理由把首夜救人对象说成"救了自己"（实际救 4 号守卫）；关键状态
  （解药已用）记忆正确，行为全部正确，仅内心理由的事实复述有误。
- villager 对身份声称的信任度仍偏低（与 9202 轮观察一致）：9311 真预言家被放逐、
  9313 真预言家被放逐。属 deepseek-v4-flash 判断质量问题。

## P1-12 村民 seer_claim 动作与 VJ 战术指令自相矛盾

**证据（9323 Day1，2 号雷昊内心理由）**："当前行动列表中 seer_claim 仅允许声明自己是
预言家，不能编造查验结果。我按战术起身跳预言家。"——VJ 战术指令要求平民"编造一份
查验结果（如给某人金水）"，但 `action_catalog._seer_claim_spec` 给 Villager 的
seer_claim 是 CLAIM_IDENTITY（仅声明身份、参数非法），两条指令打架，模型行为随选路
而异：走 seer_claim 动作就只能空跳（9323 的 2 号），走自由 talk 才能编造查验
（9303 的 4 号、9313 的 3 号）。

**对照**：已验证版本实现的发言为自由文本+诚实规则硬门（仅 B 组限制），VJ 局村民可完整
假跳（9009 局 5 号韩朴编造查验成功）。

**修复**：`_seer_claim_spec` 中 Villager 分支在廉价磋商组（honesty_rule=False）改为
CLAIM_FAKE（与狼人同规格：legal_targets=其他存活座位，结果 good/wolf）。诚实规则下
非预言家在函数入口已返回 None，B 组行为完全不变；Witch/Guard 等神职仍为仅声明身份。
测试 `test_identity_only_seer_claim_strips_check_fields` 同步改写并覆盖新行为
（villager=CLAIM_FAKE，witch/guard=CLAIM_IDENTITY）。修复后 133 passed。

### 最终轮（seeds 9321-9324，P0-9/P1-10/P2-11 生效、P1-12 修复前）

| 局 | 组 | 胜者 | 投票一致性 | 规则 | 记忆 | 截断 | ID泄漏/模板理由 |
|---|---|---|---|---|---|---|---|
| 9321 | B | village | 13/13 ✅ | 守卫挡刀救女巫/查验链闭环/ plurality ✅ | 女巫+守卫 ✅ | 无 ✅ | 无 ✅ |
| 9322 | WJ | village | 13/13 ✅ | 双狼同查杀/空刀明细"→空刀" ✅ | 女巫+守卫 ✅ | 无 ✅ | 无 ✅ |
| 9323 | VJ | wolf | 13/13 ✅ | 毒/刀/连守/parity ✅ | 女巫复述 D1 全票型 ✅ | 无 ✅ | 无（但发现 P1-12 文案矛盾） |
| 9324 | SQ | wolf | 12/12 ✅ | 奶穿/parity ✅ | 全部 ✅ | 无 ✅ | 无 ✅ |

- 机械扫描：51 票 0 理由-指向错位、0 截断、0 内部玩家 ID 泄漏。
- 9321 出现理想诚实规则闭环：女巫明确引用"未被驳回的声称即为真"条款，全场跟随
  真预言家查验链两天放逐两只狼。
- 9323 的 VJ 矛盾（P1-12）之外无任何引擎问题。

### P1-12 修复后验证（seeds 9331/9332，VJ 组两局）

- 9331：6 号平民假跳+编造金水（完整 VJ），票型 plurality 正确，0 错位 0 截断。
- 9332：1 号平民假跳+编造查验，内心理由明确"编造一个查验结果…吸引狼人刀我"，
  真预言家对跳存活 Day1；12 票 0 错位 0 截断。
- VJ 战术在结构化路径下可完整执行，模型不再输出"行动列表不允许编造"的困惑陈述。

## 验收总结

**"对局过程可以对标真实情景，不因引擎问题出现幻觉"——达成。** 三轮共 14 局、
179 张选票、56 次夜行动逐项核对：

1. **投票**：所有投票理由与落票目标一致（严格行格式下多票理由以目标名字开头，
   与引擎按座位查得的名字互相印证）；无"说投 1 结果投 2"。
2. **记忆**：女巫解药/毒药状态、守卫上夜守护对象、全员对既往票型的复述与事件流
   逐条吻合（9324 守卫复述 D1 全部 6 票逐票准确）。
3. **规则**：奶穿（同守同救）、守卫挡刀、守卫不可自守/连守、女巫解药仅救当晚刀口/
   首夜限自救、毒杀、空刀（廉价磋商组）、狼队共识收敛、plurality+平票随机放逐、
   parity/all_wolves_dead 判定——全部与引擎状态一致。
4. **文本质量**：无截断（P0-2/P0-9 修复后）、无 addendum 泄漏、无内部玩家 ID 泄漏、
   skip/空刀渲染为"（无行动）/（无目标/空刀）/空刀"。
5. **残余（模型层，非引擎）**：个别内心理由的散文复述有误（9311 守卫写错自己守护
   座位、9313 女巫把救人对象说成自己），但公开行为与行动记录全部正确；villager 对
   身份声称的信任度偏低属 deepseek-v4-flash 判断质量问题（B 组 9321 在诚实规则
   加成条款下已改善为查验链闭环）。
